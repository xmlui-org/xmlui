---
"xmlui": patch
---

Fix compiled event handlers so top-level arrow-function handlers execute instead of evaluating to an unused function value.

Log the effective script execution mode when an XMLUI app starts.
