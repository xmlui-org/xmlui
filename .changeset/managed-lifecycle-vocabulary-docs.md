---
"xmlui": patch
---

Document the managed lifecycle vocabulary (universal `onMount` / `onUnmount` / `onError` events, the declarative `<Lifecycle>` component, and the container-level `onBeforeDispose` flush hook) on the website under **Docs › Managed React › Managed Lifecycle Vocabulary**.
