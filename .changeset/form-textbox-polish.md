---
"xmlui": patch
---

Add fallback text for built-in XMLUI framework labels when the active locale has no translation, render input and button focus rings evenly on all sides, and avoid reserving FileInput button space for a missing default icon.
