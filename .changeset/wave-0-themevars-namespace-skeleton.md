---
"xmlui": patch
---

Add theme-variable namespace prefix registry (plan #02 Step 0): `components-core/themevars/` module with canonical `PackagePrefix` table for all first-party extension packages; `themeNamespacePrefix` field added to the `Extension` interface.
