---
"xmlui-docs-blocks": patch
---

Declare react-router-dom as a dependency of xmlui-docs-blocks and align it to the patched React Router release so the Blog component can resolve its router hook during extension builds.
