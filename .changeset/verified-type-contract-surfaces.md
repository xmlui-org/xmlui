---
"xmlui": patch
---

Surface verified type-contract diagnostics through the XMLUI language server, Vite plugin, and runtime expression path; add a metadata drift guard and make `strictTypeContracts` default-on with an explicit warn-only migration mode.
