---
"xmlui": patch
---

Plan #16 (W7-3, W7-4): add `determinism-floating-point-token` and `determinism-iteration-order-symbol` analyzer rules, and ship the `xmlui replay <expected.json> [actual.json]` CLI subcommand for trace-divergence checks.
