---
"xmlui": patch
---

Warn when standalone custom component files collide with built-in component names, and keep computedUses-scoped components from re-rendering on unrelated routing state object changes.
