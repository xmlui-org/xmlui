---
"xmlui": patch
---

Allow `Main.xmlui` and `App.xmlui` entrypoints, including Markdown `xmlui-pg` playground apps, to define reusable top-level components inline while preserving strict component-file parsing.
