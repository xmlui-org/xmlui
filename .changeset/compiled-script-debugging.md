---
"xmlui": patch
---

Add a shared `compileScripts` config switch for compiled XMLUI bindings and event handlers, and enable compiled script source maps automatically in dev server mode when script compilation is active.
