---
"xmlui": patch
---

Add build-validation analyzer skeleton (plan #13 Step 0): `components-core/analyzer/` module with `BuildDiagnostic` type, rule registry, walker, and suppression-comment support; `strictBuildValidation` appGlobals key documented; `kind:"build"` registered in the Inspector trace.
