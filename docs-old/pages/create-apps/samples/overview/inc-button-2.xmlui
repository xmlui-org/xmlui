<Component name="IncButton">
  <Button label="{$props.label ?? 'Click to increment'}: {count}">
    <variable name="count" value="{0}" />
    <event name="click" value="count += (parseInt($props.increment ?? 1))" />
  </Button>
</Component>
