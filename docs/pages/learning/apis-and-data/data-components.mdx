# Components with Data

_TBD_

- `datasource` property
- `DataSource` component
- `ApiBinding` component
- read operations
- mutating operations
- progress
- programmatic fetch, refresh, etc.

- loaders? Can we get rid of them?

```ueml copy
<List datasource="https://api.spacexdata.com/v3/history">
  <prop name="itemTemplate">
    <Card title="{$item.title}" subTitle="{$item.details}"/>
  </prop>
</List>
```

<Playground
  previewOnly
  name="Getting data from Spacex API"
  app={`
    <List datasource="https://api.spacexdata.com/v3/history">
      <prop name="itemTemplate">
        <Card title="{$item.title}" subTitle="{$item.details}"/>
      </prop>
    </List>
    `}
  height={300}
/>

<Playground
    previewOnly
    name="Getting data from Spacex API #2"
    app={`
    <List>
      <Datasource url="https://api.spacexdata.com/v3/history" />
      <prop name="itemTemplate">
        <Card title="{$item.title}" subTitle="{$item.details}"/>
      </prop>
    </List>
    `}
    height={300}
/>
