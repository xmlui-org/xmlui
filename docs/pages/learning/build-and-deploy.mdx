# Build and Deploy

When your app is ready for production (or distribution), XMLUI turns the source code into static files you can directly serve from a web server. In this section, you will learn how the framework supports this process.