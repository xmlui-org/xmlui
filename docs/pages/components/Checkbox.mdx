# Checkbox

The `Checkbox` component allows users to make binary choices, typically between checked or unchecked. It consists of a small box that can be toggled on or off by clicking on it.

To bind data to a \`Checkbox\`, use the XMLUI [Forms infrastructure](../learning/using-components/forms).

## Context Variables

This component does not have any context variables.

## Properties

### `autoFocus`

This property sets the label of the component.

Default value: `false`.

### `description`

(*** NOT IMPLEMENTED YET ***) This optional property displays an alternate description of the Checkbox besides its label.

### `enabled`

This boolean property value indicates whether the component responds to user events (`true`) or not (`false`).

Default value: `true`.

```ueml copy {4-5, 9-10}
<VStack margin="1rem" gap="0.8rem">
  Enabled checkboxes:
  <HStack gap="0.5rem">
    <Checkbox initialValue="true" enabled="true" />
    <Checkbox initialValue="false" enabled="true" />
  </HStack>
  Disabled checkboxes:
  <HStack gap="0.5rem">
    <Checkbox initialValue="true" enabled="false" />
    <Checkbox initilaValue="false" enabled="false" />
  </HStack>
</VStack>
```

<Playground
  previewOnly
  name="Example: enabled"
  height={160}
  app={`
    <VStack margin="1rem" gap="0.8rem">
      Enabled checkboxes:
      <HStack gap="0.5rem">
        <Checkbox initialValue="true" enabled="true" />
        <Checkbox initialValue="false" enabled="true" />
      </HStack>
      Disabled checkboxes:
      <HStack gap="0.5rem">
        <Checkbox initialValue="true" enabled="false" />
        <Checkbox initilaValue="false" enabled="false" />
      </HStack>
    </VStack>
  `}
/>

### `indeterminate`

The `true` value of this property signals that the component is in an _intedeterminate state_.

This prop is commonly used if there are several other checkboxes linked to one checkbox and some items in that group of checkboxes are in a mixed state: at least one item has a different value compared to the rest.

The following sample binds the state of two checkboxes to one and updates the state of the top checkbox accordingly. When the states of the bound checkboxes are different, the top checkbox is set to indeterminate:

```ueml copy {4}
<VStack margin="1rem" gap="0.8rem" var.indeterminate="{false}">
  <Checkbox
    label="Indeterminate Checkbox"
    indeterminate="{indeterminate}"
    initialValue="{cb1.value}"
    readOnly="true" />
  <ChangeListener
    listenTo="{ { v1: cb1.value, v2: cb2.value } }"
    onDidChange="indeterminate = cb1.value !== cb2.value" />
  Group of checkboxes:
  <HStack gap="0.5rem">
    <Checkbox label="Checkbox #1" id="cb1" initialValue="true" />
    <Checkbox label="Checkbox #2" id="cb2" initialValue="false" />
  </HStack>
</VStack>
```

Try this sample by clicking the bottom group of checkboxes.

<Playground
  previewOnly
  name="Example: indeterminate"
  height={140}
  app={`
    <VStack margin="1rem" gap="0.8rem" var.indeterminate="{false}">
      <Checkbox 
        label="Indeterminate Checkbox" 
        indeterminate="{indeterminate}" 
        initialValue="{cb1.value}"
        readOnly="true" />
      <ChangeListener
        listenTo="{ { v1: cb1.value, v2: cb2.value } }" 
        onDidChange="indeterminate = cb1.value !== cb2.value" />
      Group of checkboxes:
      <HStack gap="0.5rem">
        <Checkbox label="Checkbox #1" id="cb1" initialValue="true" />
        <Checkbox label="Checkbox #2" id="cb2" initialValue="false" />
      </HStack>
    </VStack>
  `}
/>

### `initialValue`

This property sets the component's initial value.

Default value: `false`.

### `label`

This property sets the label of the component.

```ueml copy
<VStack margin="1rem" gap="0.5rem">
  <Checkbox label="Example label" initialValue="true" />
  <Checkbox label="Another label" intialValue="false" />
</VStack>
```

<Playground
  previewOnly
  name="Example: label"
  height={90}
  app={`
    <VStack margin="1rem" gap="0.5rem">
      <Checkbox label="Example label" initialValue="true" />
      <Checkbox label="Another label" intialValue="false" />
    </VStack>
  `}
/>

### `labelId`

You can specify the identifier of a component acting as its label. When you click the label, the component behaves as you clicked it.

### `labelPosition`

Places the label at the given position of the component.

Default value: `"right"`.

Available values: `top`, `right`, `bottom`, `left`

```ueml copy
<VStack margin="1rem" gap="0.8rem">
  <Checkbox label="Top label" labelPosition="top" initialValue="true" />
  <Checkbox label="Right label" labelPosition="right" initialValue="true" />
  <Checkbox label="Bottom label" labelPosition="bottom" initialValue="true" />
  <Checkbox label="Left label" labelPosition="left" initialValue="true" />
</VStack>
```

<Playground
  previewOnly
  name="Example: labelPosition"
  height={200}
  app={`
    <VStack margin="1rem" gap="0.8rem">
      <Checkbox label="Top label" labelPosition="top" initialValue="true" />
      <Checkbox label="Right label" labelPosition="right" initialValue="true" />
      <Checkbox label="Bottom label" labelPosition="bottom" initialValue="true" />
      <Checkbox label="Left label" labelPosition="left" initialValue="true" />
    </VStack>
  `}
/>

### `readOnly`

Set this property to `true` to disallow changing the component value.

### `validationStatus`

This property allows you to set the validation status of the input component.

Available values:

| Value | Description |
| --- | --- |
| `valid` | Visual indicator for an input that is accepted |
| `warning` | Visual indicator for an input that produced a warning |
| `error` | Visual indicator for an input that produced an error |


## API

This component does not provide any API.

## Events

### `click`

This event is triggered when the Checkbox is clicked.

### `didChange`

This event is triggered when value of Checkbox has changed.

```ueml copy
<VStack margin="0.75rem" gap="0.5rem" verticalAlignment="center" var.changes="">
  <Checkbox label="Changeable" onDidChange="changes += '+'" />
  <Checkbox label="Readonly" readOnly="true" onDidChange="changes += '-'" />
  <Text value="Changes: {changes}" />
</VStack>
```

<Playground
  previewOnly
  name="Example: didChange"
  height={110}
  app={`
    <VStack margin="0.75rem" gap="0.5rem" verticalAlignment="center" var.changes="">
      <Checkbox label="Changeable" onDidChange="changes += '+'" />
      <Checkbox label="Readonly" readOnly="true" onDidChange="changes += '-'" />
      <Text value="Changes: {changes}" />
    </VStack>
  `}
/>

### `gotFocus`

This event is triggered when the Checkbox has received the focus.

Click the `Checkbox` in the example demo to change the label text. Note how clicking elsewhere resets the text to the original.

```ueml copy {4,5}
<VStack var.focused="{false}" margin="0.75rem" gap="0.5rem" verticalAlignment="center">
  <Checkbox
    value="true"
    onGotFocus="focused = true"
    onLostFocus="focused = false"
  />
  <Text value="{focused === true ? 'I am focused!' : 'I have lost the focus!'}" />
</VStack>
```

<Playground
  previewOnly
  name="Example: gotFocus/lostFocus"
  height={72}
  app={`
    <VStack var.focused="{false}" margin="0.75rem" gap="0.5rem" verticalAlignment="center">
      <Checkbox
        value="true"
        onGotFocus="focused = true"
        onLostFocus="focused = false"
      />
      <Text value="{focused === true ? 'I am focused!' : 'I have lost the focus!'}" />
    </VStack>
  `}
/>

### `lostFocus`

This event is triggered when the Checkbox has lost the focus.

(See the example above)

## Styling

The `Checkbox` component uses these theme variables to customize its appearance:

- `color-bg-Checkbox-default`
- `color-border-Checkbox-default`
- `radius-Checkbox-default`
- `color-bg-Checkbox--disabled`
- `color-bg-indicator-Checkbox`
- `color-bg-checked-Checkbox`
- `color-bg-checked-Checkbox-warning`
- `color-bg-checked-Checkbox-success`
- `color-bg-checked-Checkbox-error`
- `color-border-checked-Checkbox`
- `color-border-checked-Checkbox-warning`
- `color-border-checked-Checkbox-error`

> **Note**: some generic styling options are not available for this component.
> Namely using any `padding` features like the following:
>
> ```
> <Checkbox padding="1rem" />
> ```
>
> If you need to add space around the component, use `margins` instead of `paddings`.

### Theme Variables

| Variable | Default Value (Light) | Default Value (Dark) |
| --- | --- | --- |
| color-bg-Checkbox--disabled | $color-surface-200 | $color-surface-800 |
| color-bg-Checkbox-default |  |  |
| color-bg-Checkbox-error |  |  |
| color-bg-Checkbox-success |  |  |
| color-bg-Checkbox-warning |  |  |
| color-bg-Switch |  |  |
| color-bg-Switch--disabled |  |  |
| color-bg-checked-Checkbox | $color-primary-500 | $color-primary-400 |
| color-bg-checked-Checkbox-error | $color-border-Checkbox-error | $color-border-Checkbox-error |
| color-bg-checked-Checkbox-success | $color-border-Checkbox-success | $color-border-Checkbox-success |
| color-bg-checked-Checkbox-warning | $color-border-Checkbox-warning | $color-border-Checkbox-warning |
| color-bg-checked-Switch |  |  |
| color-bg-checked-Switch-error |  |  |
| color-bg-checked-Switch-success |  |  |
| color-bg-checked-Switch-warning |  |  |
| color-border-Checkbox--disabled |  |  |
| color-border-Checkbox-default |  |  |
| color-border-Checkbox-default--hover |  |  |
| color-border-Checkbox-error |  |  |
| color-border-Checkbox-success |  |  |
| color-border-Checkbox-warning |  |  |
| color-border-Switch |  |  |
| color-border-Switch--disabled |  |  |
| color-border-Switch-default--hover |  |  |
| color-border-Switch-error |  |  |
| color-border-Switch-success |  |  |
| color-border-Switch-warning |  |  |
| color-border-checked-Checkbox | $color-primary-500 | $color-primary-400 |
| color-border-checked-Checkbox-error | $color-border-Checkbox-error | $color-border-Checkbox-error |
| color-border-checked-Checkbox-success | $color-border-Checkbox-success | $color-border-Checkbox-success |
| color-border-checked-Checkbox-warning | $color-border-Checkbox-warning | $color-border-Checkbox-warning |
| color-border-checked-Switch |  |  |
| color-border-checked-Switch-error |  |  |
| color-border-checked-Switch-success |  |  |
| color-border-checked-Switch-warning |  |  |
| color-outline-Checkbox-default--focus |  |  |
| color-outline-Checkbox-error--focus |  |  |
| color-outline-Checkbox-success--focus |  |  |
| color-outline-Checkbox-warning--focus |  |  |
| color-outline-Switch--focus |  |  |
| offset-outline-Checkbox-default--focus |  |  |
| offset-outline-Checkbox-error--focus |  |  |
| offset-outline-Checkbox-success--focus |  |  |
| offset-outline-Checkbox-warning--focus |  |  |
| offset-outline-Switch--focus |  |  |
| radius-Checkbox-default |  |  |
| radius-Checkbox-error |  |  |
| radius-Checkbox-success |  |  |
| radius-Checkbox-warning |  |  |
| style-outline-Checkbox-default--focus |  |  |
| style-outline-Checkbox-error--focus |  |  |
| style-outline-Checkbox-success--focus |  |  |
| style-outline-Checkbox-warning--focus |  |  |
| style-outline-Switch--focus |  |  |
| thickness-outline-Checkbox-default--focus |  |  |
| thickness-outline-Checkbox-error--focus |  |  |
| thickness-outline-Checkbox-success--focus |  |  |
| thickness-outline-Checkbox-warning--focus |  |  |
| thickness-outline-Switch--focus |  |  |
| color-bg-indicator-Checkbox | $color-bg-primary | $color-bg-primary |
| color-bg-indicator-Switch |  |  |

