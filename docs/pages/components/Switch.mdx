import { Callout } from "nextra/components";

# Switch [#component-switch]

The `Switch` component is a user interface element that allows users to toggle between two states: on and off. It consists of a small rectangular or circular button that can be moved left or right to change its state.



## Properties

### `autoFocus`

If this property is set to `true`, the component gets the focus automatically when displayed.

Default value: `false`.

### `description`

(*** NOT IMPLEMENTED YET ***) This optional property displays an alternate description of the Switch besides its label.

### `enabled`

This boolean property value indicates whether the component responds to user events (`true`) or not (`false`).

Default value: `true`.

This boolean property indicates whether the checkbox responds to user events (i.e. clicks);
it is `true` by default.

```ueml copy {4-5, 9-10}
<App>
  Enabled switches:
  <HStack>
    <Switch initialValue="true" enabled="true" />
    <Switch initialValue="false" enabled="true" />
  </HStack>
  Disabled switches:
  <HStack>
    <Switch initialValue="true" enabled="false" />
    <Switch initilaValue="false" enabled="false" />
  </HStack>
</App>
```

<Playground
  name="Example: enabled"
  app={`
    <App>
      Enabled switches:
      <HStack>
        <Switch initialValue="true" enabled="true" />
        <Switch initialValue="false" enabled="true" />
      </HStack>
      Disabled switches:
      <HStack>
        <Switch initialValue="true" enabled="false" />
        <Switch initilaValue="false" enabled="false" />
      </HStack>
    </App>
  `}
/>

### `indeterminate`

The `true` value of this property signals that the component is in an _intedeterminate state_.

### `initialValue`

This property sets the component's initial value.

Default value: `false`.

### `label`

This property sets the label of the component.

This property sets the label of the component.

```ueml copy
<App>
  <Switch label="Example label" initialValue="true" />
  <Switch label="Another label" intialValue="false" />
</App>
```

<Playground
  name="Example: label"
  app={`
    <App>
      <Switch label="Example label" initialValue="true" />
      <Switch label="Another label" intialValue="false" />
    </App>
  `}
/>

### `labelBreak`

This boolean value indicates if the `Switch` labels can be split into multiple lines if it would overflow the available label width.

Default value: `false`.

### `labelId`

You can specify the identifier of a component acting as its label. When you click the label, the component behaves as you clicked it.

### `labelPosition`

Places the label at the given position of the component.

Default value: `"right"`.

Available values: `top`, `right` **(default)**, `bottom`, `left`

```ueml copy
<App>
  <Switch label="Top label" labelPosition="top" initialValue="true" />
  <Switch label="Right label" labelPosition="right" initialValue="true" />
  <Switch label="Bottom label" labelPosition="bottom" initialValue="true" />
  <Switch label="Left label" labelPosition="left" initialValue="true" />
</App>
```

<Playground
  name="Example: labelPosition"
  app={`
    <App>
      <Switch label="Top label" labelPosition="top" initialValue="true" />
      <Switch label="Right label" labelPosition="right" initialValue="true" />
      <Switch label="Bottom label" labelPosition="bottom" initialValue="true" />
      <Switch label="Left label" labelPosition="left" initialValue="true" />
    </App>
  `}
/>

### `labelWidth`

This property sets the width of the `Switch`.

### `readOnly`

Set this property to `true` to disallow changing the component value.

If true, the value of the component cannot be modified.

```ueml copy
<App>
  <Switch readOnly="true" label="Checked" initialValue="true" />
  <Switch readOnly="true" label="Unchecked" intialValue="false" />
</App>
```

<Playground
  name="Example: readOnly"
  app={`
    <App>
      <Switch readOnly="true" label="Checked" initialValue="true" />
      <Switch readOnly="true" label="Unchecked" intialValue="false" />
    </App>
  `}
/>

### `required`

Set this property to `true` to indicate it must have a value before submitting the containing form.

### `validationStatus`

This property allows you to set the validation status of the input component.

Available values:

| Value | Description |
| --- | --- |
| `valid` | Visual indicator for an input that is accepted |
| `warning` | Visual indicator for an input that produced a warning |
| `error` | Visual indicator for an input that produced an error |


## Events

### `click`

This event is triggered when the Switch is clicked.

### `didChange`

This event is triggered when value of Switch has changed.

This event is triggered when the `Switch` is toggled due to user interaction.
A read-only switch never fires this event, and it won't fire if the switch's value is set programmatically.

```ueml copy
<App verticalAlignment="center" var.changes="">
  <Switch label="Changeable" onDidChange="changes += '+'" />
  <Switch label="Readonly" readOnly="true" onDidChange="changes += '-'" />
  <Text value="Changes: {changes}" />
</App>
```

<Playground
  name="Example: didChange"
  app={`
    <App verticalAlignment="center" var.changes="">
      <Switch label="Changeable" onDidChange="changes += '+'" />
      <Switch label="Readonly" readOnly="true" onDidChange="changes += '-'" />
      <Text value="Changes: {changes}" />
    </App>
  `}
/>

### `gotFocus`

This event is triggered when the Switch has received the focus.

This event is triggered when the `Switch` receives focus.

Click the `Switch` in the example demo to change the label text. Note how clicking elsewhere resets the text to the original.

```ueml copy {4,5}
<App var.focused="{false}" verticalAlignment="center">
  <Switch
    value="true"
    onGotFocus="focused = true"
    onLostFocus="focused = false"
  />
  <Text value="{focused === true ? 'I am focused!' : 'I have lost the focus!'}" />
</App>
```

<Playground
  name="Example: gotFocus/lostFocus"
  app={`
    <App var.focused="{false}" verticalAlignment="center">
      <Switch
        value="true"
        onGotFocus="focused = true"
        onLostFocus="focused = false"
      />
      <Text value="{focused === true ? 'I am focused!' : 'I have lost the focus!'}" />
    </App>
  `}
/>

### `lostFocus`

This event is triggered when the Switch has lost the focus.

## Exposed Methods

### `setValue`

You can use this method to set the component's current value programmatically (`true`: checked, `false`: unchecked).

```ueml copy {9,12,14}
<App var.changes="">
  <Switch
    id="mySwitch"
    readOnly="true"
    label="This switch can be set only programmatically"
    onDidChange="changes += '+'" />
  <HStack>
    <Button
      label="Check"
      onClick="mySwitch.setValue(true)" />
    <Button
      label="Uncheck"
      onClick="mySwitch.setValue(false)" />
  </HStack>
  <Text>The switch is {checkbox.value ? "checked" : "unchecked"}</Text>
  <Text value="Changes: {changes}" />
</App>
```

<Playground
  name="Example: value and setValue"
  app={`
    <App var.changes="">
      <Switch
        id="mySwitch"
        readOnly="true"
        label="This switch can be set only programmatically"
        onDidChange="changes += '+'" />
      <HStack>
        <Button
          label="Check"
          onClick="mySwitch.setValue(true)" />
        <Button
          label="Uncheck"
          onClick="mySwitch.setValue(false)" />
      </HStack>
      <Text>The switch is {checkbox.value ? "checked" : "unchecked"}</Text>
      <Text value="Changes: {changes}" />
    </App>
  `}
/>

### `value`

You can query this read-only API property to query the component's current value (`true`: checked, `false`: unchecked).

## Styling

The `Switch` component uses these theme variables to customize its appearance:

- `color-bg-Switch`
- `color-border-Switch`
- `color-bg-Switch--disabled`
- `color-bg-indicator-Switch`
- `color-bg-checked-Switch`
- `color-bg-checked-Switch-warning`
- `color-bg-checked-Switch-success`
- `color-bg-checked-Switch-error`
- `color-border-checked-Switch`
- `color-border-checked-Switch-warning`
- `color-border-checked-Switch-success`
- `color-border-checked-Switch-error`

<Callout type="info" emoji="📔">
some generic styling options are not available for this component.
Namely using any `padding` features like the following:

```
<Switch padding="1rem" />
```

If you need to add space around the component, use `margins` instead of `paddings`.
</Callout>

### Theme Variables

| Variable | Default Value (Light) | Default Value (Dark) |
| --- | --- | --- |
| [color-bg](../styles-and-themes/common-units/#color)-checked-Switch | $color-primary-500 | $color-primary-400 |
| [color-bg](../styles-and-themes/common-units/#color)-checked-Switch | $color-primary-500 | $color-primary-400 |
| [color-bg](../styles-and-themes/common-units/#color)-checked-Switch-error | $color-border-Switch-error | $color-border-Switch-error |
| [color-bg](../styles-and-themes/common-units/#color)-checked-Switch-error | $color-border-Switch-error | $color-border-Switch-error |
| [color-bg](../styles-and-themes/common-units/#color)-checked-Switch-success | $color-border-Switch-success | $color-border-Switch-success |
| [color-bg](../styles-and-themes/common-units/#color)-checked-Switch-success | $color-border-Switch-success | $color-border-Switch-success |
| [color-bg](../styles-and-themes/common-units/#color)-checked-Switch-warning | $color-border-Switch-warning | $color-border-Switch-warning |
| [color-bg](../styles-and-themes/common-units/#color)-checked-Switch-warning | $color-border-Switch-warning | $color-border-Switch-warning |
| [color-bg](../styles-and-themes/common-units/#color)-indicator-Switch | $color-bg-primary | $color-bg-primary |
| [color-bg](../styles-and-themes/common-units/#color)-Switch | $color-surface-400 | $color-surface-500 |
| [color-bg](../styles-and-themes/common-units/#color)-Switch | $color-surface-400 | $color-surface-500 |
| [color-bg](../styles-and-themes/common-units/#color)-Switch--disabled | $color-surface-200 | $color-surface-800 |
| [color-bg](../styles-and-themes/common-units/#color)-Switch--disabled | $color-surface-200 | $color-surface-800 |
| [color-border](../styles-and-themes/common-units/#color)-checked-Switch | $color-primary-500 | $color-primary-400 |
| [color-border](../styles-and-themes/common-units/#color)-checked-Switch | $color-primary-500 | $color-primary-400 |
| [color-border](../styles-and-themes/common-units/#color)-checked-Switch-error | $color-border-Switch-error | $color-border-Switch-error |
| [color-border](../styles-and-themes/common-units/#color)-checked-Switch-error | $color-border-Switch-error | $color-border-Switch-error |
| [color-border](../styles-and-themes/common-units/#color)-checked-Switch-success | $color-border-Switch-success | $color-border-Switch-success |
| [color-border](../styles-and-themes/common-units/#color)-checked-Switch-success | $color-border-Switch-success | $color-border-Switch-success |
| [color-border](../styles-and-themes/common-units/#color)-checked-Switch-warning | $color-border-Switch-warning | $color-border-Switch-warning |
| [color-border](../styles-and-themes/common-units/#color)-checked-Switch-warning | $color-border-Switch-warning | $color-border-Switch-warning |
| [color-border](../styles-and-themes/common-units/#color)-Switch | $color-surface-400 | $color-surface-500 |
| [color-border](../styles-and-themes/common-units/#color)-Switch | $color-surface-400 | $color-surface-500 |
| [color-border](../styles-and-themes/common-units/#color)-Switch--disabled | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [color-border](../styles-and-themes/common-units/#color)-Switch-default--hover | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [color-border](../styles-and-themes/common-units/#color)-Switch-error | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [color-border](../styles-and-themes/common-units/#color)-Switch-success | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [color-border](../styles-and-themes/common-units/#color)-Switch-warning | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [color-outline](../styles-and-themes/common-units/#color)-Switch--focus | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [offset-outline](../styles-and-themes/common-units/#size)-Switch--focus | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [style-outline](../styles-and-themes/common-units/#border)-Switch--focus | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [thickness-outline](../styles-and-themes/common-units/#size)-Switch--focus | <GrayText>none</GrayText> | <GrayText>none</GrayText> |

