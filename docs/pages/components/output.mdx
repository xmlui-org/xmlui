import { Callout } from "nextra/components";

# output [#component-output]

<Callout type="warning" emoji="📔">This component is in an **experimental** state; you can use it in your app. However, we may modify it, and it may even have breaking changes in the future.</Callout>

This component renders an HTML `output` tag.



## Properties

### `form`

Specifies the form element that the output is associated with

### `htmlFor`

Specifies the IDs of the elements that this output is related to

### `name`

Specifies the name of the output

## Events

This component does not have any events.

## Exposed Methods

This component does not expose any methods.

## Styling

This component does not have any styles.
