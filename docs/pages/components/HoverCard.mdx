import { Callout } from "nextra/components";

# HoverCard

(**OBSOLETE**) This component displays some content when its parent component is hovered.

## Properties

### `triggerTemplate`

The component that opens the hover card when hovered.

## Events

This component does not have any events.

## Context Values

This component does not have any context values.

## Exposed Members

This component does not expose any members.

## Styling

This component does not have any theme variables.
