import { Callout } from "nextra/components";

# FormItem

<Callout type="info" emoji="ℹ️">This component is in an **experimental** state; you can use it in your app. However, we may modify it, and it may even have breaking changes in the future.</Callout>

A `FormItem` component represents a single input element within a `Form`. The value within the `FormItem` may be associated with a particular property within the encapsulating `Form` component's data.

You can learn more about this component in the [Using Forms](../learning/using-components/forms.mdx) article.

## Properties

### `autoFocus`

This property sets the label of the component.

Default value: `false`.

```ueml copy /autoFocus="true"/
<Form padding="0.5rem" 
  subject="{{ firstname: 'James', lastname: 'Clewell' }}" 
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
>
  <FormItem label="Firstname" bindTo="firstname" autoFocus="true" />
  <FormItem label="Lastname" bindTo="lastname" />
</Form>
```

<Playground
  previewOnly
  name="Example: autoFocus"
  app={`<Form padding="0.5rem" 
  subject="{{ firstname: 'James', lastname: 'Clewell' }}" 
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
>
  <FormItem label="Firstname" bindTo="firstname" autoFocus="true" />
  <FormItem label="Lastname" bindTo="lastname" />
</Form>`}
/>

### `bindTo`

This property binds a particular input field to one of the attributes of the `Form` data. It names the property of the form's `subject` data to get the input's initial value. When the field is saved, its value will be stored in the `subject` property with this name.

Try to enter some kind of text in the input field labelled `Lastname` and submit the form. Note how the submitted data is looks like compared to the one set in `subject`.

```ueml copy /bindTo="firstname"/
<Form padding="0.5rem"
  subject="{{ firstname: 'James', lastname: 'Clewell' }}" 
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
>
  <FormItem label="Firstname" bindTo="firstname" />
  <FormItem label="Lastname" />
</Form>
```

<Playground
  previewOnly
  name="Example: bindTo"
  app={`<Form padding="0.5rem" 
  subject="{{ firstname: 'James', lastname: 'Clewell' }}" 
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
>
  <FormItem label="Firstname" bindTo="firstname" />
  <FormItem label="Lastname" />
</Form>`}
/>

### `customValidationsDebounce`

This optional number prop determines the time interval between two runs of a custom validation.

The default value is `0`.

Note how changing the input in the demo below will result in a slight delay of input checks noted by the appearance of a new "I" character.

```ueml copy /customValidationsDebounce="3000"/
<Form
  var.validations="Validations: "
  subject="{{ name: 'Joe' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem
    customValidationsDebounce="3000"
    onValidate="(value) => { validations += '| '; return value === value.toUpperCase(); }"
    bindTo="name"
  />
  <Text value="{validations}" />
</Form>
```

<Playground
  previewOnly
  name="Example: customValidationsDebounce"
  app={`<Form
  var.validations="Validations: "
  subject="{{ name: 'Joe' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem
    customValidationsDebounce="3000"
    onValidate="(value) => { validations += '| '; return value === value.toUpperCase(); }"
    bindTo="name"
  />
  <Text value="{validations}" />
</Form>`}
/>

### `enabled`

This boolean property value indicates whether the component responds to user events (`true`) or not (`false`).

Default value: `true`.

```ueml copy {2-3}
<Form padding="0.5rem">
  <FormItem label="Firstname" enabled="true" />
  <FormItem label="Lastname" enabled="false" />
</Form>
```

<Playground
  previewOnly
  name="Example: enabled"
  app={`<Form padding="0.5rem">
  <FormItem label="Firstname" enabled="true" />
  <FormItem label="Lastname" enabled="false" />
</Form>`}
/>

### `initialValue`

This property sets the component's initial value.

```ueml copy {3} /initialValue/
<Form
  padding="0.5rem"
  subject="{{ firstname: 'Michael', lastname: undefined }}" 
>
  <FormItem label="Firstname" bindTo="firstname" initialValue="James" />
  <FormItem label="Lastname" bindTo="lastname" initialValue="Jordan" />
</Form>
```

<Playground
  previewOnly
  name="Example: initialValue"
  app={`<Form
  padding="0.5rem"
  subject="{{ firstname: 'Michael', lastname: undefined }}" 
>
  <FormItem label="Firstname" bindTo="firstname" initialValue="James" />
  <FormItem label="Lastname" bindTo="lastname" initialValue="Jordan" />
</Form>`}
/>

### `label`

This property sets the label of the component.

```ueml copy /label="Firstname"/
<Form>
  <FormItem label="Firstname" />
</Form>
```

<Playground
  previewOnly
  name="Example: label"
  app={`<Form padding="0.5rem">
  <FormItem label="Firstname" />
</Form>`}
/>

### `labelBreak`

This boolean value indicates if the label can be split into multiple lines if it would overflow the available label width.

### `labelPosition`

Places the label at the given position of the component.

Available values: `top`, `right`, `bottom`, `left`

Different input components have different layout methods
(i.e. `TextBox` labels are positioned at the top, `Checkbox` labels are on the right side).

```ueml copy /label="Firstname"/
<Form>
  <FormItem label="Left Label" labelPosition="left" />
  <FormItem label="Top Label" labelPosition="top" />
  <FormItem label="Right Label" labelPosition="right" />
  <FormItem label="Bottom Label" labelPosition="bottom" />
</Form>
```

<Playground
  previewOnly
  name="Example: labelPosition"
  app={`<Form padding="0.5rem">
  <FormItem label="Left Label" labelPosition="left" />
  <FormItem label="Top Label" labelPosition="top" />
  <FormItem label="Right Label" labelPosition="right" />
  <FormItem label="Bottom Label" labelPosition="bottom" />
</Form>`}
/>

### `labelWidth`

This property sets the width of the item label.

### `lengthInvalidMessage`

This optional string property is used to customize the message that is displayed on a failed length check: [minLength](#minlength) or [maxLength](#maxlength).

In the app, type a name longer than four characters in both fields, then leave the edited field. The two fields will display different error messages; the second uses the customized one.

```ueml copy /lengthInvalidMessage="Name is too long!"/
<Form
  subject="{{ firstname: 'James', lastname: 'Clewell' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem maxLength="4" bindTo="firstname" />
  <FormItem lengthInvalidMessage="Name is too long!" maxLength="4" bindTo="lastname" />
</Form>
```

<Playground
  previewOnly
  name="Example: lengthInvalidMessage"
  app={`<Form
  subject="{{ firstname: 'James', lastname: 'Clewell' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem maxLength="4" bindTo="firstname" />
  <FormItem lengthInvalidMessage="Name is too long!" maxLength="4" bindTo="lastname" />
</Form>`}
/>

### `lengthInvalidSeverity`

This property sets the severity level of length validations.

Possible values are the following: `error`, `warning`, `valid`.
The default severity level is `error`.

In the app, type a name longer than four characters in both fields, then leave the edited field. The two fields will display different error messages; the second uses a warning instead of an error.

```ueml copy /lengthInvalidSeverity="warning"/
<Form
  subject="{{ firstname: 'James', lastname: 'Clewell' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem maxLength="4" bindTo="firstname" />
  <FormItem lengthInvalidSeverity="warning" maxLength="4" bindTo="lastname" />
</Form>
```

<Playground
  previewOnly
  name="Example: lengthInvalidSeverity"
  app={`<Form
  subject="{{ firstname: 'James', lastname: 'Clewell' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem maxLength="4" bindTo="firstname" />
  <FormItem lengthInvalidSeverity="warning" maxLength="4" bindTo="lastname" />
</Form>`}
/>

### `maxLength`

Checks whether the input has a maximum length of a specified value.

Note that it is not possible for the user to enter a string larger than the value of the `maxLength`,
but setting such a value programmatically still results in a validation check.

In the demo below, try to enter an input longer than 4 characters or submit the form as is.

```ueml copy /maxLength="4"/
<Form
  subject="{{ firstname: 'James' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem maxLength="4" bindTo="firstname" />
</Form>
```

<Playground
  previewOnly
  name="Example: maxLength"
  app={`<Form
  subject="{{ firstname: 'James' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem maxLength="4" bindTo="firstname" />
</Form>`}
/>

### `maxTextLength`

The maximum length of the text in the input field

### `maxValue`

Checks whether the input has the maximum specified value.

Note that it is not possible for the user to enter a number larger than the value of the `maxValue`,
but setting such a value programmatically still results in a validation check.

In the demo below, enter an input greater than 99 or just submit the form as is.

```ueml copy /maxValue="99"/
<Form
  subject="{{ age: 100 }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem maxValue="99" bindTo="age" type="integer" />
</Form>
```

<Playground
  previewOnly
  name="Example: maxValue"
  app={`<Form
  subject="{{ age: 100 }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem maxValue="99" bindTo="age" type="integer" />
</Form>`}
/>

### `minLength`

Checks whether the input has a minimum length of a specified value.

In the demo below, enter an input shorter than 4 characters or just submit the form as is.

```ueml copy /minLength="4"/
<Form
  subject="{{ firstname: '' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem minLength="4" bindTo="firstname" />
</Form>
```

<Playground
  previewOnly
  name="Example: minLength"
  app={`<Form
  subject="{{ firstname: '' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem minLength="4" bindTo="firstname" />
</Form>`}
/>

### `minValue`

Checks whether the input has the minimum specified value.

Note that it is not possible for the user to enter a number smaller than the value of the `minValue`,
but setting such a value programmatically still results in a validation check.

In the demo below, enter an input smaller than 18 or just submit the form as is.

```ueml copy /minValue="18"/
<Form
  subject="{{ age: 0 }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem minValue="18" bindTo="age" type="integer" />
</Form>
```

<Playground
  previewOnly
  name="Example: minValue"
  app={`<Form
  subject="{{ age: 0 }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem minValue="18" bindTo="age" type="integer" />
</Form>`}
/>

### `pattern`

Checks whether the input fits a predefined regular expression.

| Value   | Description                                                                                                                                                |
| :------ | :--------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `email` | Accepts the `[username]@[second level domain].[top level domain]` format                                                                                     |
| `phone` | Accepts a wide range of characters: numbers, upper- and lowercase letters and the following symbols: `#`, `*`, `)`, `(`, `+`, `.`, `\`, `-`, `_`, `&`, `'` |
| `url`   | Accepts URLs and URIs starting with either `http` or `https`                                                                                               |

> **Note:** To define custom patterns and regular expressions, see the [regex section](#regex).

In the demo below, enter an input that is not solely one lowercase string or just submit the form as is.

```ueml copy /pattern="email"/
<Form
  subject="{{ userEmail: 'mailto' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem pattern="email" userEmail="mailto" />
</Form>
```

<Playground
  previewOnly
  name="Example: regex"
  app={`<Form
  subject="{{ userEmail: 'mailto' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem pattern="email" bindTo="userEmail" />
</Form>`}
/>

### `patternInvalidMessage`

This optional string property is used to customize the message that is displayed on a failed pattern test.

In the demo below, enter anything that does not look like an email and click outside to see the regular and custom message.

```ueml copy /patternInvalidMessage="This does not look like an email"/
<Form
  subject="{{ oldEmail: 'mailto', newEmail: 'mailto' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem pattern="email" bindTo="oldEmail" />
  <FormItem patternInvalidMessage="This does not look like an email" pattern="email" bindTo="newEmail" />
</Form>
```

<Playground
  previewOnly
  name="Example: patternInvalidMessage"
  app={`<Form
  subject="{{ oldEmail: 'mailto', newEmail: 'mailto' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem pattern="email" bindTo="oldEmail" />
  <FormItem patternInvalidMessage="This does not look like an email" pattern="email" bindTo="newEmail" />
</Form>`}
/>

### `patternInvalidSeverity`

This property sets the severity level of the pattern validation.

Possible values are the following: `error`, `warning`, `valid`.
The default severity is `error`.

In the demo below, enter a string of characters that does not look like an email to see the difference in feedback.

```ueml copy /patternInvalidSeverity="warning"/
<Form
  subject="{{ oldEmail: 'mailto', newEmail: 'mailto' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem pattern="email" bindTo="oldEmail" />
  <FormItem patternInvalidSeverity="warning" pattern="email" bindTo="newEmail" />
</Form>
```

<Playground
  previewOnly
  name="Example: patternInvalidSeverity"
  app={`<Form
  subject="{{ oldEmail: 'mailto', newEmail: 'mailto' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem pattern="email" bindTo="oldEmail" />
  <FormItem patternInvalidSeverity="warning" pattern="email" bindTo="newEmail" />
</Form>`}
/>

### `rangeInvalidMessage`

This optional string property is used to customize the message that is displayed when a value is out of range.

In the demo below, enter any value that is out of range in the input fields and click outside to see the regular and custom message.
Just submitting the form as is also produces the same error.

```ueml copy /rangeInvalidMessage="Out of range!"/
<Form
  subject="{{ age: 100, customAge: 100 }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem minValue="0" maxValue="99" bindTo="age" type="integer" />
  <FormItem minValue="0" maxValue="99" rangeInvalidMessage="Out of range!" bindTo="customAge" type="integer" />
</Form>
```

<Playground
  previewOnly
  name="Example: rangeInvalidMessage"
  app={`<Form
  subject="{{ age: 100, customAge: 100 }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem minValue="0" maxValue="99" bindTo="age" type="integer" />
  <FormItem minValue="0" maxValue="99" rangeInvalidMessage="Out of range!" bindTo="customAge" type="integer" />
</Form>`}
/>

### `rangeInvalidSeverity`

This property sets the severity level of the value range validation.

Possible values are the following: `error`, `warning`, `valid`.
The default severity is `error`.

In the demo below, enter any value that is out of range in the input fields and click outside to see the regular and custom message.
Just submitting the form as is also produces the same error.

```ueml copy /rangeInvalidSeverity="warning"/
<Form
  subject="{{ age: 100, customAge: 100 }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem minValue="0" maxValue="99" bindTo="age" type="integer" />
  <FormItem minValue="0" maxValue="99" rangeInvalidSeverity="warning" bindTo="customAge" type="integer" />
</Form>
```

<Playground
  previewOnly
  name="Example: rangeInvalidSeverity"
  app={`<Form
  subject="{{ age: 100, customAge: 100 }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem minValue="0" maxValue="99" bindTo="age" type="integer" />
  <FormItem minValue="0" maxValue="99" rangeInvalidSeverity="warning" bindTo="customAge" type="integer" />
</Form>`}
/>

### `regex`

Checks whether the input fits the provided regular expression.

In the demo below, enter an input that is not solely one lowercase string or just submit the form as is.

```ueml copy /regex="^[a-z]+$"/
<Form
  subject="{{ password: 'PASSWORD123' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem regex="^[a-z]+$" bindTo="password" />
</Form>
```

<Playground
  previewOnly
  name="Example: regex"
  app={`<Form
  subject="{{ password: 'PASSWORD123' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem regex="^[a-z]+$" bindTo="password" />
</Form>`}
/>

### `regexInvalidMessage`

This optional string property is used to customize the message that is displayed on a failed regular expression test.

In the demo below, enter a password that is not a lowercase string and click outside to see the regular and custom message.

```ueml copy /regexInvalidMessage="Password must be all lowercase"/
<Form
  subject="{{ oldPassword: 'PASSWORD123', newPassword: 'PASSWORD123' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem regex="^[a-z]+$" bindTo="oldPassword" />
  <FormItem regexInvalidMessage="Password must be all lowercase" regex="^[a-z]+$" bindTo="newPassword" />
</Form>
```

<Playground
  previewOnly
  name="Example: regexInvalidMessage"
  app={`<Form
  subject="{{ oldPassword: 'PASSWORD123', newPassword: 'PASSWORD123' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem regex="^[a-z]+$" bindTo="oldPassword" />
  <FormItem regexInvalidMessage="Password must be all lowercase" regex="^[a-z]+$" bindTo="newPassword" />
</Form>`}
/>

### `regexInvalidSeverity`

This property sets the severity level of regular expression validation.

Possible values are the following: `error`, `warning`, `valid`.
The default severity is `error`.

In the demo below, enter a password that is not a lowercase string and click outside to see the regular and custom message.
Just submitting the form as is also produces the same error.

```ueml copy /regexInvalidSeverity="warning"/
<Form
  subject="{{ oldPassword: 'PASSWORD123', newPassword: 'PASSWORD123' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem regex="^[a-z]+$" bindTo="oldPassword" />
  <FormItem regexInvalidSeverity="warning" regex="^[a-z]+$" bindTo="newPassword" />
</Form>
```

<Playground
  previewOnly
  name="Example: regexInvalidSeverity"
  app={`<Form
  subject="{{ oldPassword: 'PASSWORD123', newPassword: 'PASSWORD123' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem regex="^[a-z]+$" bindTo="oldPassword" />
  <FormItem regexInvalidSeverity="warning" regex="^[a-z]+$" bindTo="newPassword" />
</Form>`}
/>

### `required`

Set this property to `true` to indicate it must have a value before submitting the containing form.

```ueml copy /required="true"/
<Form
  subject="{{ name: undefined }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem required="true" label="Name" bindTo="name" />
</Form>
```

<Playground
  previewOnly
  name="Example: required"
  app={`<Form
  subject="{{ name: undefined }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem required="true" label="Name" bindTo="name" />
</Form>`}
/>

### `requiredInvalidMessage`

This optional string property is used to customize the message that is displayed if the field is not filled in.

In the demo below, leave the field empty and click outside to see the regular and custom message.

```ueml copy /requiredInvalidMessage="Lastname is required!"/
<Form
  subject="{{ firstname: undefined, lastname: undefined }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem required="true" label="Firstname" bindTo="firstname" />
  <FormItem requiredInvalidMessage="Lastname is required!" required="true" label="Lastname" bindTo="lastname" />
</Form>
```

<Playground
  previewOnly
  name="Example: requiredInvalidMessage"
  app={`<Form
  subject="{{ firstname: undefined, lastname: undefined }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem required="true" label="First Name" bindTo="firstname" />
  <FormItem requiredInvalidMessage="Last Name is required!" required="true" label="Last Name" bindTo="lastname" />
</Form>`}
/>

### `syncToValidation`

This boolean property indicates if the following validation properties should override the underlying controls' properties.

- `maxLength` --> Set the `maxTextLength` property value of `Text` or `TextArea` if that value is not defined.
- `minValue` --> Set the `min` property value of `NumberBox`.
- `maxValue` --> Set the `max` property value of `NumberBox`.

### `type`

This property is used to determine the specific input control the FormItem will wrap around. Note that the control names start with a lowercase letter and map to input components found in XMLUI.

| Identifier      | Renders to this component                  |
| :-------------- | :----------------------------------------- |
| `checkbox`      | Checkbox                                   |
| `combobox`      | Combobox                                   |
| `custom`        | Custom control specified in children       |
| `datePicker`    | DatePicker                                 |
| `file`          | FileInput                                  |
| `integer`       | NumberBox with `integersOnly` set to true  |
| `multiCombobox` | MultiCombobox                              |
| `multiSelect`   | MultiSelect                                |
| `number`        | NumberBox with `integersOnly` set to false |
| `radioGroup`    | RadioGroup                                 |
| `select`        | Select                                     |
| `switch`        | Switch                                     |
| `text`          | **(default)** TextBox                      |
| `textarea`      | Textarea                                   |

> **Note:** For custom controls, there is no need to explicitly set the `type` to `custom`.
> Omitting the type and providing child components implicitly sets it to custom.

### `validationMode`

This property sets what kind of validation mode or strategy to employ for a particular input field.

| Value         | Description                                                                                                                                         |
| :------------ | :-------------------------------------------------------------------------------------------------------------------------------------------------- |
| `errorLate`   | **(default)** Display the error when the field loses focus. If an error is already displayed, continue for every keystroke until input is accepted. |
| `onChanged`   | Display error (if present) for every keystroke.                                                                                                     |
| `onLostFocus` | Show/hide error (if present) only if the field loses focus.                                                                                         |

## Events

### `validate`

This event is used to define a custom validation function.

In the demo below, leave the field as is and submit the form or enter an input that is not all capital letters.

```ueml copy {8}
<Form
  subject="{{ name: 'James' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem
    bindTo="name"
    onValidate="(value) => value === value.toUpperCase()"
  />
</Form>
```

<Playground
  previewOnly
  name="Example: validate"
  app={`<Form
  subject="{{ name: 'James' }}"
  onSubmit="(toSave) => window.alert(JSON.stringify(toSave))"
  padding="0.5rem"
>
  <FormItem
    bindTo="name"
    onValidate="(value) => value === value.toUpperCase()"
  />
</Form>`}
/>

## Context Values

### `$setValue`

This function can be invoked to set the `FormItem` instance's value. The function has a single argument, the new value to set.

### `$validationResult`

This variable represents the result of the latest validation of the `FormItem` instance.

### `$value`

The context variable represents the current value of the `FormItem`. It can be used in expressions and code snippets within the `FormItem` instance.

## Exposed Members

This component does not expose any members.

## Styling

`FormItem` supports the styling of labels through these theme variables:

- `color-text-FormItemLabel`
- `font-size-FormItemLabel`
- `font-weight-FormItemLabel`
- `font-style-FormItemLabel`
- `text-transform-FormItemLabel`
- `color-text-FormItemLabel-required`
- `font-size-FormItemLabel-required`
- `font-weight-FormItemLabel-required`
- `font-style-FormItemLabel-required`
- `text-transform-FormItemLabel-required`
- `color-text-FormItemLabel-requiredMark`

This component displays other input components and binds them with the form's logic. You can style the individual form components; see the [`type` property](#type) to check what components are used beyond a particular form item.

### Theme Variables

| Variable | Default Value (Light) | Default Value (Dark) |
| --- | --- | --- |
| [color-text](../styles-and-themes/common-units/#color-values)-FormItemLabel | $color-text-primary | $color-text-primary |
| [color-text](../styles-and-themes/common-units/#color-values)-FormItemLabel-required | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [color-text](../styles-and-themes/common-units/#color-values)-FormItemLabel-requiredMark | $color-danger-400 | $color-danger-400 |
| [font-size](../styles-and-themes/common-units/#size-values)-FormItemLabel | $font-size-small | $font-size-small |
| [font-size](../styles-and-themes/common-units/#size-values)-FormItemLabel-required | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| font-style-FormItemLabel | normal | normal |
| font-style-FormItemLabel-required | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [font-weight](../styles-and-themes/common-units/#font-weight-values)-FormItemLabel | $font-weight-medium | $font-weight-medium |
| [font-weight](../styles-and-themes/common-units/#font-weight-values)-FormItemLabel-required | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| text-transform-FormItemLabel | none | none |
| text-transform-FormItemLabel-required | <GrayText>none</GrayText> | <GrayText>none</GrayText> |

