import { Callout } from "nextra/components";

import data from  "../../component-samples/Table/list-component-data.js";

# Table [#component-table]

`Table` is a component that displays cells organized into rows and columns. The `Table` component is virtualized so it only renders visible cells.



In the following sections the examples use data with the structure outlined below:

| Id   | Name    | Quantity | Unit   | Category   |
| :--- | :------ | :------- | :----- | :--------- |
| 0    | Apples  | 5        | pieces | fruits     |
| 1    | Bananas | 6        | pieces | fruits     |
| 2    | Carrots | 100      | grams  | vegetables |
| 3    | Spinach | 1        | bunch  | vegetables |
| 4    | Milk    | 10       | liter  | diary      |
| 5    | Cheese  | 200      | grams  | diary      |

The data is provided as JSON. In the source code samples, the `data={[...]}` declaration represents the data above.

All samples use table columns with the following definition unless noted otherwise
(The `...` declaration nested into `<Table>` represents this column definition):

```ueml copy
<Table data='{[...]}'>
  <Column bindTo="name"/>
  <Column bindTo="quantity"/>
  <Column bindTo="unit"/>
</Table>
```

> **Note**: See the [`TableColumnDef` reference](./TableColumnDef) to learn more about table columns.

## Properties

### `alwaysShowSelectionHeader (default: false)`

This property indicates when the row selection header is displayed. When the value is `true,` the selection header is always visible. Otherwise, it is displayed only when hovered.

### `autoFocus (default: false)`

If this property is set to `true`, the component gets the focus automatically when displayed.

### `data`

The component receives data via this property. The `data` property is a list of items that the `Table` can display.

```ueml copy /data='{[...]}'/
<App>
  <Table data='{[...]}'>
    <Column bindTo="name"/>
    <Column bindTo="quantity"/>
    <Column bindTo="unit"/>
  </Table>
</App>
```

<Playground
  name="Example: data"
  app={`
    <App>
      <Table data='{${JSON.stringify(data, null, 2)}}'>
        <Column bindTo="name"/>
        <Column bindTo="quantity"/>
        <Column bindTo="unit"/>
      </Table>
    </App>
  `}
/>

You can also provide the `Table` with data directly from an API via this property.
Here, the component displays rocket information coming from the official SpaceX API.

```ueml copy /data='https://api.spacexdata.com/v3/rockets'/
<App>
  <Table data='https://api.spacexdata.com/v3/rockets'>
    <Column header="Image" size="140px">
      <Image height="100px" fit="cover" src="{$item.flickr_images[0]}"/>
    </Column>
    <Column canSort="true" bindTo="country"/>
    <Column canSort="true" bindTo="company"/>
  </Table>
</App>
```

<Playground
  name="Example: data API Call"
  height={400}
  app={`
    <App>
      <Table data='https://api.spacexdata.com/v3/rockets'>
        <Column header="Image" size="140px">
          <Image height="100px" fit="cover" src="{$item.flickr_images[0]}"/>
        </Column>
        <Column canSort="true" bindTo="country"/>
        <Column canSort="true" bindTo="company"/>
      </Table>
    </App>
  `}
/>

### `enableMultiRowSelection`

This boolean property indicates whether you can select multiple rows in the table. This property only has an effect when the rowsSelectable property is set. Setting it to `false` limits selection to a single row.

This boolean property indicates whether you can select multiple rows in the table.
This property only has an effect when the `rowsSelectable` property is set.
Setting it to `false` limits selection to a single row.

By default, the value of this property is `true`.

```ueml copy /enableMultiRowSelection="false"/
<App>
  <Table data='{[...]}' 
    rowsSelectable="true" 
    enableMultiRowSelection="false">
    <Column bindTo="name"/>
    <Column bindTo="quantity"/>
    <Column bindTo="unit"/>
  </Table>
</App>
```

<Playground
  name="Example: enableMultiRowSelection"
  app={`
    <App>
      <Table data='{${JSON.stringify(data, null, 2)}}' 
        rowsSelectable="true" 
        enableMultiRowSelection="false">
        <Column bindTo="name"/>
        <Column bindTo="quantity"/>
        <Column bindTo="unit"/>
      </Table>
    </App>
  `}
/>

### `headerHeight`

This optional property is used to specify the height of the table header.

It accepts common [size values](./appearance/common-units.mdx#size-values).

```ueml copy /headerHeight="60px"/
<App>
  <Table data='{[...]}' headerHeight="60px">
    <Column bindTo="name"/>
    <Column bindTo="quantity"/>
    <Column bindTo="unit"/>
  </Table>
</App>
```

<Playground
  name="Example: headerHeight"
  app={`
    <App>
      <Table data='{${JSON.stringify(data, null, 2)}}' headerHeight="60px">
        <Column bindTo="name"/>
        <Column bindTo="quantity"/>
        <Column bindTo="unit"/>
      </Table>
    </App>
  `}
/>

### `hideHeader`

Set the header visibility using this property. Set it to `true` to hide the header.

Set the header visibility using this property. Set it to `true` to hide the header.
The default value is `false`.

```ueml copy /hideHeader="true"/
<App>
  <Table data='{[...]}' hideHeader="true">
    <Column bindTo="name"/>
    <Column bindTo="quantity"/>
    <Column bindTo="unit"/>
  </Table>
</App>
```

<Playground
  name="Example: hideHeader"
  height={300}
  app={`
    <App>
      <Table data='{${JSON.stringify(data, null, 2)}}' hideHeader="true">
        <Column bindTo="name"/>
        <Column bindTo="quantity"/>
        <Column bindTo="unit"/>
      </Table>
    </App>
  `}
/>

### `iconNoSort`

Allows the customization of the icon displayed in the Table column header when when sorting is enabled and sorting is not done according to the column.

Allows the customization of the icon displayed in a Table column header when when sorting is enabled
and sorting is not done according to the column. Use the "-" (dash) value to sign that you do not want to display an icon when a table column is not sorted.

```ueml copy /iconNoSort="close"/
<App>
  <Table data='{[...]}' sortBy="quantity" iconNoSort="close">
    <Column bindTo="name" canSort="true" />
    <Column bindTo="quantity" canSort="true" />
    <Column bindTo="unit" canSort="true" />
  </Table>
</App>
```

<Playground
  name="Example: iconNoSort"
  app={`
    <App>
      <Table data='{${JSON.stringify(data, null, 2)}}' sortBy="quantity" iconNoSort="close">
        <Column bindTo="name" canSort="true" />
        <Column bindTo="quantity" canSort="true" />
        <Column bindTo="unit" canSort="true" />
      </Table>
    </App>
  `}
/>

### `iconSortAsc`

Allows the customization of the icon displayed in the Table column header when sorting is enabled, sorting is done according to the column, and the column is sorted in ascending order.

Allows the customization of the icon displayed in a Table column header when sorting is enabled,
sorting is done according to the column, and the column is sorted in ascending order.

```ueml copy /iconSortAsc="chevronup"/
<App>
  <Table data='{[...]}' sortBy="quantity" iconSortAsc="chevronup">
    <Column bindTo="name" canSort="true" />
    <Column bindTo="quantity" canSort="true" />
    <Column bindTo="unit" canSort="true" />
  </Table>
</App>
```

<Playground
  name="Example: iconSortAsc"
  app={`
    <App>
      <Table data='{${JSON.stringify(data, null, 2)}}' sortBy="quantity" iconSortAsc="chevronup">
        <Column bindTo="name" canSort="true" />
        <Column bindTo="quantity" canSort="true" />
        <Column bindTo="unit" canSort="true" />
      </Table>
    </App>
  `}
/>

### `iconSortDesc`

Allows the customization of the icon displayed in the Table column header when sorting is enabled, sorting is done according to the column, and the column is sorted in descending order.

Allows the customization of the icon displayed in a Table column header when sorting is enabled,
sorting is done according to the column, and the column is sorted in descending order.

```ueml copy /iconSortDesc="chevrondown"/
<App>
  <Table data='{[...]}' sortBy="quantity" iconSortDesc="chevrondown">
    <Column bindTo="name" canSort="true" />
    <Column bindTo="quantity" canSort="true" />
    <Column bindTo="unit" canSort="true" />
  </Table>
</App>
```

Select a column header and set it to descending ordering.

<Playground
  name="Example: iconSortDesc"
  app={`
    <App>
      <Table data='{${JSON.stringify(data, null, 2)}}' sortBy="quantity" iconSortDesc="chevrondown">
        <Column bindTo="name" canSort="true" />
        <Column bindTo="quantity" canSort="true" />
        <Column bindTo="unit" canSort="true" />
      </Table>
    </App>
  `}
/>

### `isPaginated`

This property adds pagination controls to the `Table`.

```ueml copy /isPaginated="true"/
<App>
  <Table data='{[...]}' isPaginated="true" pageSizes="{[3, 6, 12]}">
    <Column bindTo="name"/>
    <Column bindTo="quantity"/>
    <Column bindTo="unit"/>
  </Table>
</App>
```

<Playground
  name="Example: isPaginated"
  app={`
    <App>
      <Table data='{${JSON.stringify(data, null, 2)}}' isPaginated="true" pageSizes="{[3, 6, 12]}">
        <Column bindTo="name"/>
        <Column bindTo="quantity"/>
        <Column bindTo="unit"/>
      </Table>
    </App>
  `}
/>

### `items`

You can use `items` as an alias for the `data` property. When you bind the table to a data source (e.g. an API endpoint), the `data` acts as the property that accepts a URL to fetch information from an API. When both `items` and `data` are used, `items` has priority.

### `loading`

This boolean property indicates if the component is fetching (or processing) data. This property is useful when data is loaded conditionally or receiving it takes some time.

This boolean property indicates if the component is fetching (or processing) data.
This property is useful when data is loaded conditionally or receiving it takes some time.

```ueml copy
<App>
  <Table loading="true">
    <Column bindTo="name"/>
    <Column bindTo="quantity"/>
  </Table>
</App>
```

<Playground
  name="Example: loading"
  app={`
    <App>
      <Table loading="true">
        <Column bindTo="name"/>
        <Column bindTo="quantity"/>
      </Table>
    </App>
  `}
/>

### `noDataTemplate`

A property to customize what to display if the table does not contain any data.

```ueml copy {3-5}
<App>
  <Table>
    <property name="noDataTemplate">
      <Text value="No data loaded" variant="strong" />
    </property>
    <Column bindTo="name"/>
    <Column bindTo="quantity"/>
  </Table>
</App>
```

<Playground
  name="Example: noDataTemplate"
  app={`
    <App>
      <Table>
        <property name="noDataTemplate">
          <Text value="No data loaded" variant="strong" />
        </property>
        <Column bindTo="name"/>
        <Column bindTo="quantity"/>
      </Table>
    </App>
  `}
/>

### `pageSizes`

Describes how big a page should be for pagination.

Page sizes are only accepted in an array, even if the array contains one item.

Note that this property only works if the [`isPaginated`](#ispaginated) property is set to `true`.

```ueml copy /pageSizes="{[3, 6, 12]}"/
<App>
  <Table data='{[...]}' isPaginated="true" pageSizes="{[3, 6, 12]}">
    <Column bindTo="name"/>
    <Column bindTo="quantity"/>
    <Column bindTo="unit"/>
  </Table>
</App>
```

<Playground
  name="Example: pageSizes"
  app={`
    <App>
      <Table data='{${JSON.stringify(data, null, 2)}}' isPaginated="true" pageSizes="{[3, 6, 12]}">
        <Column bindTo="name"/>
        <Column bindTo="quantity"/>
        <Column bindTo="unit"/>
      </Table>
    </App>
  `}
/>

### `rowDisabledPredicate`

This property defines a predicate function with a return value that determines if the row should be disabled. The function retrieves the item to display and should return a Boolean-like value.

The following example disables all table rows where the item's quantity exceeds 6:

```ueml copy {3}
<App>
  <Table data='{[...]}'
    rowDisabledPredicate="{(item) => item.quantity > 6}">
    <Column bindTo="name"/>
    <Column bindTo="quantity"/>
    <Column bindTo="unit"/>
  </Table>
</App>
```

<Callout type="info" emoji="📔">
Disabled items are rendered with a different color.
</Callout>

<Playground
  name="Example: rowDisabledPredicate"
  app={`
    <App>
      <Table data='{${JSON.stringify(data, null, 2)}}'
        rowDisabledPredicate="{(item) => item.quantity > 6}">
        <Column bindTo="name"/>
        <Column bindTo="quantity"/>
        <Column bindTo="unit"/>
      </Table>
    </App>
  `}
/>

### `rowsSelectable`

Indicates whether the rows are selectable (`true`) or not (`false`).

The default value is `false`.

```ueml copy /rowsSelectable="true"/
<App>
  <Table data='{[...]}' rowsSelectable="true">
    <Column bindTo="name"/>
    <Column bindTo="quantity"/>
    <Column bindTo="unit"/>
  </Table>
</App>
```

<Playground
  name="Example: rowsSelectable"
  app={`
    <App>
      <Table data='{${JSON.stringify(data, null, 2)}}' rowsSelectable="true">
        <Column bindTo="name"/>
        <Column bindTo="quantity"/>
        <Column bindTo="unit"/>
      </Table>
    </App>
  `}
/>

### `sortBy`

This property is used to determine which data attributes to sort by.

```ueml copy /sortBy="quantity"/
<App>
  <Table data='{[...]}' sortBy="quantity">
    <Column bindTo="name"/>
    <Column bindTo="quantity"/>
    <Column bindTo="unit"/>
  </Table>
</App>
```

<Playground
  name="Example: sortBy"
  app={`
    <App>
      <Table data='{${JSON.stringify(data, null, 2)}}' sortBy="quantity">
        <Column bindTo="name"/>
        <Column bindTo="quantity"/>
        <Column bindTo="unit"/>
      </Table>
    </App>
  `}
/>

### `sortDirection`

This property determines the sort order to be `ascending` or `descending`. This property only works if the [`sortBy`](#sortby) property is also set.

```ueml copy /sortDirection="descending"/
<App>
  <Table data='{[...]}' sortBy="quantity" sortDirection="descending">
    <Column bindTo="name"/>
    <Column bindTo="quantity"/>
    <Column bindTo="unit"/>
  </Table>
</App>
```

<Playground
  name="Example: sortDirection"
  app={`
    <App>
      <Table data='{${JSON.stringify(data, null, 2)}}' sortBy="quantity" sortDirection="descending">
        <Column bindTo="name"/>
        <Column bindTo="quantity"/>
        <Column bindTo="unit"/>
      </Table>
    </App>
  `}
/>

## Events

### `selectionDidChange`

This event is triggered when the table's current selection (the rows selected) changes. Its parameter is an array of the selected table row items. 

Of course, if multiple-row selection is not allowed (`enableMultipleRowSelection` is false), this array will contain zero or one item.

```ueml copy {4}
<App var.selection="">
  <Text>Current selection (row IDs): [{selection}]</Text>
  <Table data='{[...]}'
    rowsSelectable="true"
    enableMultiRowSelection="true"
    onSelectionDidChange="(newSel) => selection = newSel.map(item => item.id).join()" >
    <Column bindTo="name" canSort="true"/>
    <Column bindTo="quantity" canSort="true"/>
    <Column bindTo="unit" canSort="true"/>
  </Table>
</App>
```

Click on any of the column headers to trigger a new sorting:

<Playground
  name="Example: selectionDidChange"
  app={`
    <App var.selection="">
      <Text>Current selection (row IDs): [{selection}]</Text>
      <Table data='{${JSON.stringify(data, null, 2)}}'
        rowsSelectable="true"
        enableMultiRowSelection="true"
        onSelectionDidChange="(newSel) => selection = newSel.map(item => item.id).join()" >
        <Column bindTo="name" canSort="true"/>
        <Column bindTo="quantity" canSort="true"/>
        <Column bindTo="unit" canSort="true"/>
      </Table>
    </App>
  `}
/>

### `sortingDidChange`

This event is fired when the table data sorting has changed. It has two arguments: the column's name and the sort direction. When the column name is empty, the table displays the data list as it received it.

Note the [`canSort`](./TableColumnDef.mdx#cansort) properties on the `TableColumnDef` components which enable custom ordering.

```ueml copy {4}
<App var.sortedBy="">
  <Heading level="h4" value="Table is sorted by: {sortedBy || ''}" paddingLeft="1rem"/>
  <Table data='{[...]}'
    onSortingDidChange="(by, dir) => sortedBy = (by && dir) ? by + ' | ' + dir : '' " >
    <Column bindTo="name" canSort="true"/>
    <Column bindTo="quantity" canSort="true"/>
    <Column bindTo="unit" canSort="true"/>
  </Table>
</App>
```

Click on any of the column headers to trigger a new sorting:

<Playground
  name="Example: sortingDidChange"
  app={`
    <App var.sortedBy="">
      <Heading level="h4" value="Table is sorted by: {sortedBy || ''}" paddingLeft="1rem"/>
      <Table data='{${JSON.stringify(data, null, 2)}}'
        onSortingDidChange="(by, dir) => sortedBy = (by && dir) ? by + ' | ' + dir : '' " >
        <Column bindTo="name" canSort="true"/>
        <Column bindTo="quantity" canSort="true"/>
        <Column bindTo="unit" canSort="true"/>
      </Table>
    </App>
  `}
/>

### `willSort`

This event is fired before the table data is sorted. It has two arguments: the column's name and the sort direction. When the method returns a literal `false` value (and not any other falsy one), the method indicates that the sorting should be aborted.

The following example uses the `willSort` event to refuse sorting by name:

```ueml copy {4}
<App var.sortedBy="">
  <Heading level="h4" value="Table is sorted by: {sortedBy || ''}" paddingLeft="1rem"/>
  <Table data='{[...]}'
    onWillSort="(by, dir) => by !== 'name'"
    onSortingDidChange="(by, dir) => sortedBy = (by && dir) ? by + ' | ' + dir : '' " >
    <Column bindTo="name" canSort="true"/>
    <Column bindTo="quantity" canSort="true"/>
    <Column bindTo="unit" canSort="true"/>
  </Table>
</App>
```

Click on any of the column headers to trigger the event.
Though sorting is enabled in the `TableColumnnDef` component of the "name" column via `canSort`,
clicking that column header still does not sort because `willSort` prevents it:

<Playground
  name="Example: willSort"
  app={`
    <App var.sortedBy="">
      <Heading level="h4" value="Table is sorted by: {sortedBy || ''}" paddingLeft="1rem"/>
      <Table data='{${JSON.stringify(data, null, 2)}}'
        onWillSort="(by, dir) => by !== 'name'"
        onSortingDidChange="(by, dir) => sortedBy = (by && dir) ? by + ' | ' + dir : '' " >
        <Column bindTo="name" canSort="true"/>
        <Column bindTo="quantity" canSort="true"/>
        <Column bindTo="unit" canSort="true"/>
      </Table>
    </App>
  `}
/>

## Exposed Methods

### `clearSelection`

This method clears the list of currently selected table rows.

```ueml copy /clearSelection()/ /selectId(1)/ /selectId([2, 4])/ /selectAll()/
<App>
  <HStack>
    <Button label="Select all" onClick="table.selectAll()" />
    <Button label="Clear all" onClick="table.clearSelection()" />
    <Button label="Select 1" onClick="table.selectId(1)" />
    <Button label="Select 2, 4" onClick="table.selectId([2, 4])" />
  </HStack>
  <Table id="table" data='{${JSON.stringify(data, null, 2)}}'
    rowsSelectable="true"
    enableMultiRowSelection="true">
    <Column bindTo="name" canSort="true"/>
    <Column bindTo="quantity" canSort="true"/>
    <Column bindTo="unit" canSort="true"/>
  </Table>
</App>
```

<Playground
  name="Example: selection methods"
  app={`
    <App>
      <HStack>
        <Button label="Select all" onClick="table.selectAll()" />
        <Button label="Clear all" onClick="table.clearSelection()" />
        <Button label="Select 1" onClick="table.selectId(1)" />
        <Button label="Select 2, 4" onClick="table.selectId([2, 4])" />
      </HStack>
      <Table id="table" data='{${JSON.stringify(data, null, 2)}}'
        rowsSelectable="true"
        enableMultiRowSelection="true">
        <Column bindTo="name" canSort="true"/>
        <Column bindTo="quantity" canSort="true"/>
        <Column bindTo="unit" canSort="true"/>
      </Table>
    </App>
  `}
/>

### `getSelectedIds`

This method returns the list of currently selected table rows IDs.

(See the [example](#clearselection) at the `clearSelection` method)

### `getSelectedItems`

This method returns the list of currently selected table rows items.

(See the [example](#clearselection) at the `clearSelection` method)

### `selectAll`

This method selects all the rows in the table. This method has no effect if the rowsSelectable property is set to `false`.

(See the [example](#clearselection) at the `clearSelection` method)

### `selectId`

This method selects the row with the specified ID. This method has no effect if the `rowsSelectable` property is set to `false`. The method argument can be a single id or an array of them.

(See the [example](#clearselection) at the `clearSelection` method)

## Styling

The `Table` component uses the following theme variables.

Overall Table theme variables:

- `color-bg-Table`
- `style-border-cell-Table`
- `color-bg-pagination-Table`
- `color-text-Table`
- `color-text-pagination-Table`

Row theme variables:

- `color-bg-row-Table`
- `color-bg-row-Table--hover`
- `font-size-row-Table`
- `font-weight-row-Table`

Selected row theme variables:

- `color-bg-selected-Table`
- `color-bg-selected-Table--hover`

Cell theme variables:

- `border-cell-Table`
- `color-border-cell-Table`
- `thickness-border-cell-Table`
- `padding-cell-Table`
- `padding-horizontal-cell-Table`
- `padding-vertical-cell-Table`
- `padding-horizontal-cell-first-Table`
- `padding-horizontal-cell-last-Table`

Heading theme variables:

- `color-bg-heading-Table`
- `color-bg-heading-Table--hover`
- `color-bg-heading-Table--active`
- `color-text-heading-Table`
- `font-size-heading-Table`
- `font-weight-heading-Table`
- `padding-heading-Table`
- `padding-horizontal-heading-Table`
- `padding-vertical-heading-Table`
- `transform-text-heading-Table`

### Example

```json copy
{
  "id": "custom",
  "name": "Custom Theme",
  "themeVars": {
    "color-bg-heading-Table": "#c4c4c4",
    "color-bg-Table": "#ededed",
    "padding-vertical-heading-Table": "1rem",
    "color-text-Table": "black",
    "color-text-heading-Table": "black"
  }
}
```

<Playground
  name="Example: styling"
  defaultTheme="custom"
  themes={[
    {
      id: "custom",
      name: "Custom Theme",
      themeVars: {
        "color-bg-heading-Table": "#c4c4c4",
        "color-bg-Table": "#ededed",
        "padding-vertical-heading-Table": "1rem",
        "color-text-Table": "black",
        "color-text-heading-Table": "black"
      },
    },
  ]}
  app={`
    <App>
      <Table data='{${JSON.stringify(data, null, 2)}}'>
        <Column bindTo="name"/>
        <Column bindTo="quantity"/>
        <Column bindTo="unit"/>
      </Table>
    </App>
  `}
/>

### Theme Variables

| Variable | Default Value (Light) | Default Value (Dark) |
| --- | --- | --- |
| [border](../styles-and-themes/common-units/#border)-cell-Table | $thickness-border-cell-Table $style-border-cell-Table $color-border-cell-Table | $thickness-border-cell-Table $style-border-cell-Table $color-border-cell-Table |
| [color-bg](../styles-and-themes/common-units/#color)-heading-Table | $color-surface-100 | $color-surface-950 |
| [color-bg](../styles-and-themes/common-units/#color)-heading-Table--active | $color-surface-300 | $color-surface-700 |
| [color-bg](../styles-and-themes/common-units/#color)-heading-Table--hover | $color-surface-200 | $color-surface-800 |
| [color-bg](../styles-and-themes/common-units/#color)-pagination-Table | $color-bg-Table | $color-bg-Table |
| [color-bg](../styles-and-themes/common-units/#color)-row-Table | inherit | inherit |
| [color-bg](../styles-and-themes/common-units/#color)-row-Table--hover | $color-primary-50 | $color-primary-900 |
| [color-bg](../styles-and-themes/common-units/#color)-selected-Table | $color-primary-100 | $color-primary-800 |
| [color-bg](../styles-and-themes/common-units/#color)-selected-Table--hover | $color-bg-row-Table--hover | $color-bg-row-Table--hover |
| [color-bg](../styles-and-themes/common-units/#color)-Table | $color-bg | $color-bg |
| [color-border](../styles-and-themes/common-units/#color)-cell-Table | $color-border | $color-border |
| [color-outline](../styles-and-themes/common-units/#color)-heading-Table--focus | $color-outline--focus | $color-outline--focus |
| [color-text](../styles-and-themes/common-units/#color)-heading-Table | $color-surface-500 | <GrayText>none</GrayText> |
| [color-text](../styles-and-themes/common-units/#color)-pagination-Table | $color-secondary | $color-secondary |
| [color-text](../styles-and-themes/common-units/#color)-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [font-size](../styles-and-themes/common-units/#size)-heading-Table | $font-size-tiny | $font-size-tiny |
| [font-size](../styles-and-themes/common-units/#size)-row-Table | $font-size-small | $font-size-small |
| [font-weight](../styles-and-themes/common-units/#font-weight)-heading-Table | $font-weight-bold | $font-weight-bold |
| [font-weight](../styles-and-themes/common-units/#font-weight)-row-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [offset-outline](../styles-and-themes/common-units/#size)-heading-Table--focus | $offset-outline--focus | $offset-outline--focus |
| [padding-bottom](../styles-and-themes/common-units/#size)-cell-first-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding-bottom](../styles-and-themes/common-units/#size)-cell-last-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding-bottom](../styles-and-themes/common-units/#size)-cell-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding-bottom](../styles-and-themes/common-units/#size)-heading-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding](../styles-and-themes/common-units/#size)-cell-first-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding](../styles-and-themes/common-units/#size)-cell-last-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding](../styles-and-themes/common-units/#size)-cell-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding](../styles-and-themes/common-units/#size)-heading-Table | $padding-vertical-heading-Table $padding-horizontal-heading-Table | $padding-vertical-heading-Table $padding-horizontal-heading-Table |
| [padding-horizontal](../styles-and-themes/common-units/#size)-cell-first-Table | $space-5 | $space-5 |
| [padding-horizontal](../styles-and-themes/common-units/#size)-cell-last-Table | $space-5 | $space-5 |
| [padding-horizontal](../styles-and-themes/common-units/#size)-cell-Table | $space-2 | $space-2 |
| [padding-horizontal](../styles-and-themes/common-units/#size)-heading-Table | $space-2 | $space-2 |
| [padding-left](../styles-and-themes/common-units/#size)-cell-first-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding-left](../styles-and-themes/common-units/#size)-cell-last-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding-left](../styles-and-themes/common-units/#size)-cell-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding-left](../styles-and-themes/common-units/#size)-heading-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding-right](../styles-and-themes/common-units/#size)-cell-first-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding-right](../styles-and-themes/common-units/#size)-cell-last-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding-right](../styles-and-themes/common-units/#size)-cell-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding-right](../styles-and-themes/common-units/#size)-heading-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding-top](../styles-and-themes/common-units/#size)-cell-first-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding-top](../styles-and-themes/common-units/#size)-cell-last-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding-top](../styles-and-themes/common-units/#size)-cell-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding-top](../styles-and-themes/common-units/#size)-heading-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding-vertical](../styles-and-themes/common-units/#size)-cell-first-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding-vertical](../styles-and-themes/common-units/#size)-cell-last-Table | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [padding-vertical](../styles-and-themes/common-units/#size)-cell-Table | $space-2 | $space-2 |
| [padding-vertical](../styles-and-themes/common-units/#size)-heading-Table | $space-2 | $space-2 |
| [style-border](../styles-and-themes/common-units/#border-style)-cell-Table | solid | solid |
| [style-outline](../styles-and-themes/common-units/#border)-heading-Table--focus | $style-outline--focus | $style-outline--focus |
| [thickness-border](../styles-and-themes/common-units/#size)-cell-Table | 1px | 1px |
| [thickness-outline](../styles-and-themes/common-units/#size)-heading-Table--focus | $thickness-outline--focus | $thickness-outline--focus |
| [transform](../styles-and-themes/common-units/#text-transform)-text-heading-Table | uppercase | uppercase |

