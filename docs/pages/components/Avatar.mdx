import { Callout } from "nextra/components";
import pages from "../../meta/pages.js";

# Avatar

The `Avatar` component represents a user, group (or other entity's) avatar with a small image or initials.

## Properties

### `name`

This property sets the name value the Avatar uses to display initials.

```ueml copy
<Avatar name="John, Doe" />
```

<Playground
  previewOnly
  height={88}
  name="Example: name"
  app={`<VStack padding="$space-2"><Avatar name="John, Doe" /></VStack>`}
/>

### `size`

This property defines the display size of the Avatar.

Default value: `"sm"`.

Available values:

| Value | Description |
| --- | --- |
| `xs` | Extra small button |
| `sm` | Small button **(default)** |
| `md` | Medium button |
| `lg` | Large button |


```ueml copy
<HStack gap="1rem" >
  <Avatar name="Dorothy Ellen Fuller" />
  <Avatar name="Xavier Schiller" size="xs" />
  <Avatar name="Sebastien Moore" size="sm" />
  <Avatar name="Molly Dough" size="md" />
  <Avatar name="Lynn Gilbert" size="lg" />
</HStack>
```

<Playground
  previewOnly
  height={136}
  name="Example: size"
  app={`
    <HStack gap="1rem" padding="$space-2">
      <Avatar name="Dorothy Ellen Fuller" />
      <Avatar name="Xavier Schiller" size="xs" />
      <Avatar name="Sebastien Moore" size="sm" />
      <Avatar name="Molly Dough" size="md" />
      <Avatar name="Lynn Gilbert" size="lg" />
    </HStack>
  `}
/>

### `url`

This property specifies the URL of the image to display in the Avatar.

<Playground
  previewOnly
  height={108}
  name="Example: url"
  app={`<VStack padding="$space-2"><Avatar url="https://i.pravatar.cc/100" size="md" /></VStack>`}
/>

## Events

### `click`

This event is triggered when the avatar is clicked.

```ueml copy
<HStack verticalAlignment="center">   
  <Avatar name="Molly Dough" size="md" onClick="window.alert('Avatar clicked')" />
    Click the avatar!
</HStack>
```

<Playground
  previewOnly
  name="Example: click"
  app={`
    <HStack gap="1rem" verticalAlignment="center" padding="$space-2">   
      <Avatar name="Molly Dough" size="md" onClick="window.alert('Avatar clicked')" />
      Click the avatar!
    </HStack>
  `}
  height={108}
/>

## Context Values

This component does not have any context values.

## Exposed Members

This component does not expose any members.

## Styling

The `Avatar` component uses these theme variables to customize its appearance:

- `color-bg`
- `color-border`
- `color-text`
- `font-weight`
- `radius`
- `shadow`
- `style-border`
- `thickness-border`

### Theme Variables

| Variable | Default Value (Light) | Default Value (Dark) |
| --- | --- | --- |
| border-Avatar | $thickness-border-Avatar $style-border-Avatar $color-border-Avatar | $thickness-border-Avatar $style-border-Avatar $color-border-Avatar |
| [color-bg](../styles-and-themes/common-units/#color-values)-Avatar | $color-surface-100 | $color-surface-800 |
| [color-border](../styles-and-themes/common-units/#color-values)-Avatar | $color-surface-400A80 | $color-surface-700 |
| [color-text](../styles-and-themes/common-units/#color-values)-Avatar | $color-text-secondary | $color-text-secondary |
| [font-weight](../styles-and-themes/common-units/#font-weight-values)-Avatar | $font-weight-bold | $font-weight-bold |
| [radius](../styles-and-themes/common-units/#border-rounding)-Avatar | 4px | 4px |
| [shadow](../styles-and-themes/common-units/#color-values)-Avatar | inset 0 0 0 1px rgba(4,32,69,0.1) | inset 0 0 0 1px rgba(4,32,69,0.1) |
| [style-border](../styles-and-themes/common-units/#border-style-values)-Avatar | solid | solid |
| [thickness-border](../styles-and-themes/common-units/#size-values)-Avatar | 0px | 0px |

