import { Callout } from "nextra/components";

# ProgressBar [#component-progressbar]

A `ProgressBar` component visually represents the progress of a task or process.



## Properties

### `value`

This property defines the progress value with a number between 0 and 1.

The following example demonstrates using it:

```ueml copy {2-6}
<App>
  <ProgressBar value="0"/>
  <ProgressBar value="0.2"/>
  <ProgressBar value="0.6"/>
  <ProgressBar value="1"/>
  <ProgressBar value="1.2"/>
</App>
```

<Playground
  name="Example: value"
  app={`
    <App>
      <ProgressBar value="0"/>
      <ProgressBar value="0.2"/>
      <ProgressBar value="0.6"/>
      <ProgressBar value="1"/>
      <ProgressBar value="1.2"/>
    </App>
  `}
/>

## Events

This component does not have any events.

## Exposed Methods

This component does not expose any methods.

## Styling

The `ProgressBar` component uses these theme variables to customize its appearance:

- `color-bg-ProgressBar`
- `color-indicator-ProgressBar`
- `radius-ProgressBar`
- `radius-indicator-ProgressBar`
- `thicness-ProgressBar`

This sample shows how you can style `ProgressBar`:

```ueml copy
<Theme
  color-bg-ProgressBar="cyan"
  color-indicator-ProgressBar="purple"
  thickness-ProgressBar="12px"
  radius-indicator-ProgressBar="12px"
  radius-Progressbar="4px">
  <App>
    <ProgressBar value="0"/>
    <ProgressBar value="0.2"/>
    <ProgressBar value="0.6"/>
    <ProgressBar value="1"/>
    <ProgressBar value="1.2"/>
  </App>
</Theme>
```

<Playground
  name="Example: styling ProgressBar"
  app={`
    <Theme
      color-bg-ProgressBar="cyan"
      color-indicator-ProgressBar="purple"
      thickness-ProgressBar="12px"
      radius-indicator-ProgressBar="12px"
      radius-Progressbar="4px">
      <App>
        <ProgressBar value="0"/>
        <ProgressBar value="0.2"/>
        <ProgressBar value="0.6"/>
        <ProgressBar value="1"/>
        <ProgressBar value="1.2"/>
      </App>
    </Theme>
  `}
/>

### Theme Variables

| Variable | Default Value (Light) | Default Value (Dark) |
| --- | --- | --- |
| [color-bg](../styles-and-themes/common-units/#color)-ProgressBar | $color-surface-200 | $color-surface-700 |
| [color](../styles-and-themes/common-units/#color)-indicator-ProgressBar | $color-primary-500 | $color-primary-500 |
| [radius](../styles-and-themes/common-units/#border-rounding)-indicator-ProgressBar | 0px | 0px |
| [radius](../styles-and-themes/common-units/#border-rounding)-ProgressBar | $radius | $radius |
| [thickness](../styles-and-themes/common-units/#size)-ProgressBar | $space-2 | $space-2 |

