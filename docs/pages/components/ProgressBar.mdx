# ProgressBar

A `ProgressBar` component visually represents the progress of a task or process.

## Context Variables

This component does not have any context variables.

## Properties

### `value`

This property defines the progress value with a number between 0 and 1.

The following example demonstrates using it:

```ueml copy {2-6}
<VStack padding="1rem" gap="1rem">
  <ProgressBar value="0"/>
  <ProgressBar value="0.2"/>
  <ProgressBar value="0.6"/>
  <ProgressBar value="1"/>
  <ProgressBar value="1.2"/>
</VStack>
```

<Playground
  name="Example: value"
  previewOnly={true}
  height={140}
  app={`
    <VStack padding="1rem" gap="1rem">
      <ProgressBar value="0"/>
      <ProgressBar value="0.2"/>
      <ProgressBar value="0.6"/>
      <ProgressBar value="1"/>
      <ProgressBar value="1.2"/>
    </VStack>
  `}
/>

## API

This component does not provide any API.

## Events

This component does not have any events.

## Styling

The `ProgressBar` component uses these theme variables to customize its appearance:

- `color-bg-ProgressBar`
- `color-indicator-ProgressBar`
- `radius-ProgressBar`
- `radius-indicator-ProgressBar`
- `thicness-ProgressBar`

This sample shows how you can style `ProgressBar`:

```ueml copy
<Theme
  color-bg-ProgressBar="cyan"
  color-indicator-ProgressBar="purple"
  thickness-ProgressBar="12px"
  radius-indicator-ProgressBar="12px"
  radius-Progressbar="4px">
  <VStack padding="1rem" gap="1rem">
    <ProgressBar value="0"/>
    <ProgressBar value="0.2"/>
    <ProgressBar value="0.6"/>
    <ProgressBar value="1"/>
    <ProgressBar value="1.2"/>
  </VStack>
</Theme>
```

<Playground
  name="Example: styling ProgressBar"
  previewOnly={true}
  height={160}
  app={`
    <Theme
      color-bg-ProgressBar="cyan"
      color-indicator-ProgressBar="purple"
      thickness-ProgressBar="12px"
      radius-indicator-ProgressBar="12px"
      radius-Progressbar="4px">
      <VStack padding="1rem" gap="1rem">
        <ProgressBar value="0"/>
        <ProgressBar value="0.2"/>
        <ProgressBar value="0.6"/>
        <ProgressBar value="1"/>
        <ProgressBar value="1.2"/>
      </VStack>
    </Theme>
  `}
/>

### Theme Variables

| Variable | Default Value (Light) | Default Value (Dark) |
| --- | --- | --- |
| color-bg-ProgressBar | $color-surface-200 | $color-surface-700 |
| color-indicator-ProgressBar | $color-primary-500 | $color-primary-500 |
| radius-ProgressBar | $radius | $radius |
| radius-indicator-ProgressBar | 0px | 0px |
| thickness-ProgressBar | $space-2 | $space-2 |

