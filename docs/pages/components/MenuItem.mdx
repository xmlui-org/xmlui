import { Callout } from "nextra/components";

# MenuItem [#component-menuitem]

This property represents a leaf item in a menu hierarchy. Clicking the item triggers an action.

See the [`DropdownMenu` component](./DropdownMenu) for using this component in menus.

## Properties

### `active`

This property indicates if the specified menu item is active.

```ueml copy /active="true"/
<DropdownMenu label="DropdownMenu">
  <MenuItem icon="drive" active="true">Item 1</MenuItem>
  <MenuItem icon="trash">Item 2</MenuItem>
  <MenuItem icon="email">Item 3</MenuItem>
</DropdownMenu>
```

<Playground
  name="Example: icon"
  previewOnly={true}
  height={200}
  app={`
    <VStack padding="1rem">
      <DropdownMenu label="DropdownMenu">
        <MenuItem icon="drive" active="true">Item 1</MenuItem>
        <MenuItem icon="trash">Item 2</MenuItem>
        <MenuItem icon="email">Item 3</MenuItem>
      </DropdownMenu>
    </VStack>
  `}
/>

### `icon`

This property names an optional icon to display with the menu item.

```ueml copy {2-4}
<DropdownMenu label="DropdownMenu">
  <MenuItem icon="drive">Item 1</MenuItem>
  <MenuItem icon="trash">Item 2</MenuItem>
  <MenuItem icon="email">Item 3</MenuItem>
</DropdownMenu>
```

<Playground
  name="Example: icon"
  previewOnly={true}
  height={200}
  app={`
    <VStack padding="1rem">
      <DropdownMenu label="DropdownMenu">
        <MenuItem icon="drive">Item 1</MenuItem>
        <MenuItem icon="trash">Item 2</MenuItem>
        <MenuItem icon="email">Item 3</MenuItem>
      </DropdownMenu>
    </VStack>
  `}
/>

### `iconPosition`

This property allows you to determine the position of the icon displayed in the menu item.

Available values:

| Value | Description |
| --- | --- |
| `start` | The icon will appear at the start (left side when the left-to-right direction is set) |
| `end` | The icon will appear at the end (right side when the left-to-right direction is set) |


This property allows you to determine the position of the icon displayed in the menu item.

Avaliable values are:

- `start`: The icon starts the menu item (default).
- `end`: The icon is displayed after the menu item.


```ueml copy {2-4}
<DropdownMenu label="DropdownMenu">
  <MenuItem icon="drive" iconPosition="start">Item 1</MenuItem>
  <MenuItem icon="trash" iconPosition="end">Item 2</MenuItem>
  <MenuItem icon="email">Item 3</MenuItem>
</DropdownMenu>
```

<Playground
  name="Example: iconPosition"
  previewOnly={true}
  height={200}
  app={`
    <VStack padding="1rem">
      <DropdownMenu label="DropdownMenu">
        <MenuItem icon="drive" iconPosition="start">Item 1</MenuItem>
        <MenuItem icon="trash" iconPosition="end">Item 2</MenuItem>
        <MenuItem icon="email">Item 3</MenuItem>
      </DropdownMenu>
    </VStack>
  `}
/>

### `label`

This property sets the label of the component.

### `to`

This property defines the URL of the menu item. If this property is defined (and the `click` event does not have an event handler), clicking the menu item navigates to this link.

## Events

### `click`

This event is triggered when the MenuItem is clicked.

This event is fired when the user clicks the menu item. With an event handler, you can define how to respond to the user's click. If this event does not have an associated event handler but the `to` property has a value, clicking the component navigates the URL set in `to`.

If both properties are defined, `click` takes precedence.

```ueml copy {2-4}
<DropdownMenu label="DropdownMenu">
  <MenuItem onClick="window.alert('Item 1 clicked')">Item 1</MenuItem>
  <MenuItem onClick="window.alert('Item 2 clicked')">Item 2</MenuItem>
  <MenuItem onClick="window.alert('Item 3 clicked')">Item 3</MenuItem>
</DropdownMenu>
```

<Playground
  name="Example: iconPosition"
  previewOnly={true}
  height={200}
  app={`
    <VStack padding="1rem">
      <DropdownMenu label="DropdownMenu">
        <MenuItem onClick="window.alert('Item 1 clicked')">Item 1</MenuItem>
        <MenuItem onClick="window.alert('Item 2 clicked')">Item 2</MenuItem>
        <MenuItem onClick="window.alert('Item 3 clicked')">Item 3</MenuItem>
      </DropdownMenu>
    </VStack>
  `}
/>

## Context Values

This component does not have any context values.

## Exposed Members

This component does not expose any members.

## Styling

The `MenuItem` component uses these theme variables to customize its appearance:
- `color`
- `color-bg`
- `font-family`
- `font-size`
- `gap`: The gap between the item label and icon
- `padding-horizontal`
- `padding-vertical`

The `color` and `color-bg` theme variables have variants for the `--hover` state.

### Theme Variables

| Variable | Default Value (Light) | Default Value (Dark) |
| --- | --- | --- |
| [color](../styles-and-themes/common-units/#color-values)-MenuItem | $color-text-primary | $color-text-primary |
| [color](../styles-and-themes/common-units/#color-values)-MenuItem--active | $color-primary | $color-primary |
| [color](../styles-and-themes/common-units/#color-values)-MenuItem--active--hover | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [color](../styles-and-themes/common-units/#color-values)-MenuItem--hover | inherit | inherit |
| [color-bg](../styles-and-themes/common-units/#color-values)-MenuItem | $color-bg-dropdown-item | $color-bg-dropdown-item |
| [color-bg](../styles-and-themes/common-units/#color-values)-MenuItem--active | $color-bg-dropdown-item--active | $color-bg-dropdown-item--active |
| [color-bg](../styles-and-themes/common-units/#color-values)-MenuItem--active--hover | <GrayText>none</GrayText> | <GrayText>none</GrayText> |
| [color-bg](../styles-and-themes/common-units/#color-values)-MenuItem--hover | $color-bg-dropdown-item--hover | $color-bg-dropdown-item--hover |
| font-family-MenuItem | $font-family | $font-family |
| [font-size](../styles-and-themes/common-units/#size-values)-MenuItem | $font-size-small | $font-size-small |
| gap-MenuItem | $space-2 | $space-2 |
| [padding-horizontal](../styles-and-themes/common-units/#size-values)-MenuItem | $space-3 | $space-3 |
| [padding-vertical](../styles-and-themes/common-units/#size-values)-MenuItem | $space-2 | $space-2 |

