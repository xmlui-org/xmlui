# Select

A dropdown list of options

## Using `Select`

The component accepts `Option` components as children defining a particular option's label-value pair.
`Option` requires a `value` property and while also having a `label` that is displayed in the list.
If the `label` is not specified `value` is shown.

```ueml copy
<Select>
  <Option value="opt1" label="first"/>
  <Option value="opt2" label="second"/>
  <Option value="opt3" label="third"/>
</Select>
```

<Playground
  previewOnly
  name="Example: using Select"
  height={170}
  app={`
    <Select>
      <Option value="opt1" label="first"/>
      <Option value="opt2" label="second"/>
      <Option value="opt3" label="third"/>
    </Select>
  `}
/>

## Context Variables

### `$item`

This context variable acts as a template for an item in the list.

Access attributes of the item using the dot notation.

For an example, see the [\`optionTemplate\`](#optiontemplate) property or the [\`didChange\`](#didchange) event.

## Properties

### `autoFocus`

This property sets the label of the component.

Default value: `false`.

### `emptyListTemplate`

This optional property provides the ability to customize what is displayed when the list of options is empty.

Click on the second field to see the custom empty list indicator.

```ueml copy {9-11}
<VStack gap="$space-1">
  <VStack gap="$space-0_5">
    <Text value="Default:" />
    <Select />
  </VStack>
  <VStack gap="$space-0_5">
    <Text value="Custom:" />
    <Select>
      <prop name="emptyListTemplate">
        <Text variant="strong" value="Nothing to see here!" />
      </prop>
    </Select>
  </VStack>
</VStack>
```

<Playground
  previewOnly
  name="Example: emptyListTemplate"
  height={220}
  app={`
    <VStack gap="$space-1">
      <VStack gap="$space-0_5rem">
        <Text value="Default:" />
        <Select />
      </VStack>
      <VStack gap="$space-0_5rem">
        <Text value="Custom:" />
        <Select>
          <prop name="emptyListTemplate">
            <Text variant="strong" value="Nothing to see here!" />
          </prop>
        </Select>
      </VStack>
    </VStack>
  `}
/>

### `enabled`

This boolean property value indicates whether the component responds to user events (`true`) or not (`false`).

Default value: `true`.

```ueml copy
<Select enabled="false" />
```

<Playground
  previewOnly
  name="Example: enabled"
  height={60}
  app={`
    <Select enabled="false" />
  `}
/>

### `initialValue`

This property sets the component's initial value.

```ueml copy
<Select initialValue="opt3">
  <Option value="opt1" label="first"/>
  <Option value="opt2" label="second"/>
  <Option value="opt3" label="third"/>
</Select>
```

<Playground
  previewOnly
  name="Example: initialValue"
  height={170}
  app={`
    <Select initialValue="opt3">
      <Option value="opt1" label="first"/>
      <Option value="opt2" label="second"/>
      <Option value="opt3" label="third"/>
    </Select>
  `}
/>

### `labelId`

You can specify the identifier of a component acting as its label. When you click the label, the component behaves as you clicked it.

### `maxLength`

This property sets the maximum length of the input it accepts.

### `optionTemplate`

This property enables the customization of list items. To access the attributes of a list item use the `$item` context variable.

```ueml copy
<Select>
  <prop name="optionTemplate">
    <HStack verticalAlignment="center" gap="$space-0_5">
      <Icon name="info" />
      <Text value="{$item.label}" variant="strong" />
    </HStack>
  </prop>
  <Option value="opt1" label="first"/>
  <Option value="opt2" label="second"/>
  <Option value="opt3" label="third"/>
</Select>
```

<Playground
  previewOnly
  name="Example: optionTemplate"
  height={170}
  app={`
    <Select>
      <prop name="optionTemplate">
        <HStack verticalAlignment="center" gap="$space-0_5">
          <Icon name="info" />
          <Text value="{$item.label}" variant="strong" />
        </HStack>
      </prop>
      <Option value="opt1" label="first"/>
      <Option value="opt2" label="second"/>
      <Option value="opt3" label="third"/>
    </Select>
  `}
/>

### `placeholder`

A placeholder text that is visible in the input field when its empty.

```ueml copy
<Select placeholder="Please select an item">
  <Option value="opt1" label="first"/>
  <Option value="opt2" label="second"/>
  <Option value="opt3" label="third"/>
</Select>
```

<Playground
  previewOnly
  name="Example: placeholder"
  height={170}
  app={`
    <Select placeholder="Please select an item">
      <Option value="opt1" label="first"/>
      <Option value="opt2" label="second"/>
      <Option value="opt3" label="third"/>
    </Select>
  `}
/>

### `readOnly`

Set this property to `true` to disallow changing the component value.

### `required`

Set this property to `true` to indicate it must have a value before submitting the containing form.

### `validationStatus`

This property allows you to set the validation status of the input component.

Available values:

| Value | Description |
| --- | --- |
| `valid` | Visual indicator for an input that is accepted |
| `warning` | Visual indicator for an input that produced a warning |
| `error` | Visual indicator for an input that produced an error |


```ueml copy
<VStack gap="$space-0_5rem">
  <Select />
  <Select validationStatus="valid" />
  <Select validationStatus="warning" />
  <Select validationStatus="error" />
</VStack>
```

<Playground
  previewOnly
  name="Example: validationStatus"
  height={200}
  app={`<VStack gap="$space-0_5">
  <Select />
  <Select validationStatus="valid" />
  <Select validationStatus="warning" />
  <Select validationStatus="error" />
</VStack>`}
/>

## API

This component does not provide any API.

## Events

### `didChange`

This event is triggered when value of Select has changed.

```ueml copy /onDidChange="(newItem) => newValue = newItem"/
<VStack padding="$space-1" gap="$space-0_5">
  <var name="newValue" value="" />
  <Text value="{newValue}" />
  <Select onDidChange="(newItem) => newValue = newItem">
    <Option value="opt1" label="first"/>
    <Option value="opt2" label="second"/>
    <Option value="opt3" label="third"/>
  </Select>
</VStack>
```

<Playground
  previewOnly
  name="Example: didChange"
  height={200}
  app={`
  <VStack padding="$space-1" gap="$space-0_5">
    <var name="newValue" value="" />
    <Text value="{newValue}" />
    <Select onDidChange="(newItem) => newValue = newItem">
      <Option value="opt1" label="first"/>
      <Option value="opt2" label="second"/>
      <Option value="opt3" label="third"/>
    </Select>
  </VStack>
  `}
/>

### `gotFocus`

This event is triggered when the Select has received the focus.

```ueml copy {5-6}
<VStack padding="$space-1" gap="$space-0_5">
  <var name="isFocused" value="{false}" />
  <Text value="Input control is focused: {isFocused}" />
  <Select
    onGotFocus="isFocused = true"
    onLostFocus="isFocused = false"
  >
    <Option value="opt1" label="first"/>
    <Option value="opt2" label="second"/>
    <Option value="opt3" label="third"/>
  </Select>
</VStack>
```

<Playground
  previewOnly
  name="Example: gotFocus/lostFocus"
  height={200}
  app={`
  <VStack padding="$space-1" gap="$space-0_5">
    <var name="isFocused" value="{false}" />
    <Text value="Input control is focused: {isFocused}" />
    <Select
      onGotFocus="isFocused = true"
      onLostFocus="isFocused = false"
    >
      <Option value="opt1" label="first"/>
      <Option value="opt2" label="second"/>
      <Option value="opt3" label="third"/>
    </Select>
  </VStack>
  `}
/>

### `lostFocus`

This event is triggered when the Select has lost the focus.

## Styling

The `Select` component uses these theme variables:

- `min-height-Select`

These variables have 4 variants depending on the [validationStatus](#validationstatus) denoted by `|status|`.
If the status segment is omitted, the style will be applied to all statuses.

- `radius-Select-|status|`
- `font-size-Select-|status|`
- `color-placeholder-Select-|status|`
- `thickness-border-Select-|status|`

- `thickness-outline-Select-|status|--focus`
- `color-outline-Select-|status|--focus`
- `style-outline-Select-|status|--focus`
- `offset-outline-Select-|status|--focus`

```json copy
"radius-Select-default": "10px",
"color-placeholder-Select-error": "crimson"
```

In addition to the validation statuses,
the variables below are associated with styles for `hover` and `disabled` interaction states denoted by `|interact|` in their signature:

- `color-border-Select-|status|--|interact|`
- `color-bg-Select-|status|--|interact|`
- `shadow-Select-|status|--|interact|`
- `color-text-Select-|status|--|interact|`

```json copy
"color-bg-Select-default--disabled": "gray",
"color-text-Select-error--active": "lightcoral"
```

Theme variables to set the dropdown menu styles:

- `color-bg-menu-Select`
- `shadow-menu-Select`
- `radius-menu-Select`
- `color-text-indicator-Select`

Theme variables to set the selection styles in a dropdown menu:

- `color-text-value-Select`
- `color-bg-value-Select`

Theme variables to set the styles of an unselected item:

- `color-bg-item-Select--hover`
- `color-bg-item-Select--active`
- `color-text-item-Select--disabled`

### Example

```json copy
"radius-Select": "30px",
"color-placeholder-Select": "red",
"thickness-border-Select": "4px"
```

<Playground
  name="Example: styling"
  previewOnly
  height={90}
  defaultTheme="custom"
  themes={[
    {
      id: "custom",
      name: "Custom Theme",
      themeVars: {
        "radius-Select": "30px",
        "color-placeholder-Select": "red",
        "thickness-border-Select": "4px",
      },
    },
  ]}
  app={`<Select placeholder="Please select an option" />`}
/>

To style all input controls not just this component, use `Input` instead of the `Select` segment:

```
color-border-Input: "#0033FF"
```

`Input` affects the following controls:

- [`Checkbox`](./Checkbox.mdx)
- [`NumberBox`](./NumberBox.mdx)
- [`RadioGroupOption`](./RadioGroupOption.mdx)
- [`Switch`](./Switch.mdx)
- [`TextArea`](./TextArea.mdx)
- [`TextBox`](./TextBox.mdx)
- [`MultiSelect`](./MultiSelect.mdx)
- [`MultiCombobox`](./MultiCombobox.mdx)

See [custom themes](../learning/themes/overview.mdx) for styling details.

### Theme Variables

| Variable | Default Value (Light) | Default Value (Dark) |
| --- | --- | --- |
| color-bg-Select--disabled |  |  |
| color-bg-Select-default |  |  |
| color-bg-Select-default--hover |  |  |
| color-bg-Select-error |  |  |
| color-bg-Select-error--hover |  |  |
| color-bg-Select-success |  |  |
| color-bg-Select-success--hover |  |  |
| color-bg-Select-warning |  |  |
| color-bg-Select-warning--hover |  |  |
| color-bg-item-Select--active |  |  |
| color-bg-item-Select--hover | $color-bg-dropdown-item--active | $color-bg-dropdown-item--active |
| color-bg-menu-Select | $color-bg-primary | $color-bg-primary |
| color-border-Select--disabled |  |  |
| color-border-Select-default |  |  |
| color-border-Select-default--hover |  |  |
| color-border-Select-error |  |  |
| color-border-Select-error--hover |  |  |
| color-border-Select-success |  |  |
| color-border-Select-success--hover |  |  |
| color-border-Select-warning |  |  |
| color-border-Select-warning--hover |  |  |
| color-outline-Select-default--focus |  |  |
| color-outline-Select-error--focus |  |  |
| color-outline-Select-success--focus |  |  |
| color-outline-Select-warning--focus |  |  |
| color-placeholder-Select-default |  |  |
| color-placeholder-Select-error |  |  |
| color-placeholder-Select-success |  |  |
| color-placeholder-Select-warning |  |  |
| color-text-Select--disabled |  |  |
| color-text-Select-default |  |  |
| color-text-Select-default--hover |  |  |
| color-text-Select-error |  |  |
| color-text-Select-error--hover |  |  |
| color-text-Select-success |  |  |
| color-text-Select-success--hover |  |  |
| color-text-Select-warning |  |  |
| color-text-Select-warning--hover |  |  |
| color-text-indicator-Select |  |  |
| color-text-item-Select--disabled | $color-surface-200 | $color-surface-800 |
| font-size-Select |  |  |
| font-size-Select-default |  |  |
| font-size-Select-error |  |  |
| font-size-Select-success |  |  |
| font-size-Select-warning |  |  |
| offset-outline-Select-default--focus |  |  |
| offset-outline-Select-error--focus |  |  |
| offset-outline-Select-success--focus |  |  |
| offset-outline-Select-warning--focus |  |  |
| radius-Select-default |  |  |
| radius-Select-error |  |  |
| radius-Select-success |  |  |
| radius-Select-warning |  |  |
| radius-menu-Select | $radius | $radius |
| shadow-Select-default |  |  |
| shadow-Select-default--hover |  |  |
| shadow-Select-error |  |  |
| shadow-Select-error--hover |  |  |
| shadow-Select-success |  |  |
| shadow-Select-success--hover |  |  |
| shadow-Select-warning |  |  |
| shadow-Select-warning--hover |  |  |
| shadow-menu-Select | $shadow-md | $shadow-md |
| style-border-Select-default |  |  |
| style-border-Select-error |  |  |
| style-border-Select-success |  |  |
| style-border-Select-warning |  |  |
| style-outline-Select-default--focus |  |  |
| style-outline-Select-error--focus |  |  |
| style-outline-Select-success--focus |  |  |
| style-outline-Select-warning--focus |  |  |
| thickness-border-Select-default |  |  |
| thickness-border-Select-error |  |  |
| thickness-border-Select-success |  |  |
| thickness-border-Select-warning |  |  |
| thickness-outline-Select-default--focus |  |  |
| thickness-outline-Select-error--focus |  |  |
| thickness-outline-Select-success--focus |  |  |
| thickness-outline-Select-warning--focus |  |  |

