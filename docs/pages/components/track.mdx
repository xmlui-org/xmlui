import { Callout } from "nextra/components";

# track [#component-track]

<Callout type="warning" emoji="📔">This component is in an **experimental** state; you can use it in your app. However, we may modify it, and it may even have breaking changes in the future.</Callout>

This component renders an HTML `track` tag.



## Properties

### `default`

Specifies that the track is to be enabled if no other track is more suitable

### `kind`

Specifies the kind of text track

### `label`

Specifies the title of the text track

### `src`

Specifies the URL of the track file

### `srcLang`

Specifies the language of the track text data

## Events

This component does not have any events.

## Exposed Methods

This component does not expose any methods.

## Styling

This component does not have any styles.
