import { Callout } from "nextra/components";

# FadeAnimation [#component-fadeanimation]

<Callout type="warning" emoji="📔">This component is in an **experimental** state; you can use it in your app. However, we may modify it, and it may even have breaking changes in the future.</Callout>

The `FadeAnimation` component represents an animation that fades the content with adjustable opacity values.

## Properties

### `animateWhenInView`

Indicates whether the animation should start when the component is in view

```ueml copy /animateWhenInView/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeAnimation id="anim" from="0" to="1" animateWhenInView>
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeAnimation>
</App>
```

<Playground
  name="Example: animateWhenInView"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <FadeAnimation id="anim" from="0" to="1" animateWhenInView>
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </FadeAnimation>
  </App>
  `}
/>

### `delay (default: 0)`

The delay before the animation starts in milliseconds

```ueml copy /delay="1000"/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeAnimation id="anim" from="0" to="1" delay="1000">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeAnimation>
</App>
```

<Playground
  name="Example: delay"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <FadeAnimation id="anim" from="0" to="1" delay="1000">
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </FadeAnimation>
  </App>
  `}
/>

### `duration`

The duration of the animation in milliseconds

```ueml copy /duration/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeAnimation id="anim" from="0" to="1" duration="3000">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeAnimation>
</App>
```

<Playground
  name="Example: duration"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <FadeAnimation id="anim" from="0" to="1" duration="3000">
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </FadeAnimation>
  </App>
  `}
/>

### `from (default: 0)`

Sets the initial opacity of the content.If the `to` property is not set, the initial opacity set here will be used as the final opacity.

```ueml copy /from="0.5"/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeAnimation id="anim" from="0.5">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeAnimation>
</App>
```

<Playground
  name="Example: from"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <FadeAnimation id="anim" from="0.5">
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </FadeAnimation>
  </App>
  `}
/>

### `loop (default: false)`

Indicates whether the animation should loop

```ueml copy /loop/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeAnimation id="anim" from="0" to="1" loop>
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeAnimation>
</App>
```

<Playground
  name="Example: loop"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <FadeAnimation id="anim" from="0" to="1" loop>
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </FadeAnimation>
  </App>
  `}
/>

### `reverse (default: false)`

Indicates whether the animation should run in reverse

```ueml copy /reverse/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeAnimation id="anim" from="0" to="1" reverse>
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeAnimation>
</App>
```

<Playground
  name="Example: reverse"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <FadeAnimation id="anim" from="0" to="1" reverse>
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </FadeAnimation>
  </App>
  `}
/>

### `to (default: 1)`

Sets the final opacity of the content.If the `from` property is not set, the initial opacity set here will be used as the final opacity.

```ueml copy /to="0.5"/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeAnimation id="anim" to="0.5">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeAnimation>
</App>
```

<Playground
  name="Example: to"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <FadeAnimation id="anim" to="0.5">
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </FadeAnimation>
  </App>
  `}
/>

## Events

### `started`

Event fired when the animation starts

```ueml copy /onStarted="toast('Animation start')"/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeAnimation id="anim" from="0" to="1" onStarted="toast('Animation start')">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeAnimation>
</App>
```

<Playground
  name="Example: started"
  app={`
    <App>
      <Button onClick="anim.start()">Start</Button>
      <FadeAnimation id="anim" from="0" to="1" onStarted="toast('Animation start')">
        <Image
          src="/resources/images/components/image/breakfast.jpg" width="500"/>
      </FadeAnimation>
    </App>
  `}
/>

### `stopped`

Event fired when the animation stops

```ueml copy /onStopped="toast('Animation start')"/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeAnimation id="anim" from="0" to="1" onStopped="toast('Animation end')">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeAnimation>
</App>
```

<Playground
  name="Example: stopped"
  app={`
    <App>
      <Button onClick="anim.start()">Start</Button>
      <FadeAnimation id="anim" from="0" to="1" onStopped="toast('Animation end')">
        <Image
          src="/resources/images/components/image/breakfast.jpg" width="500"/>
      </FadeAnimation>
    </App>
  `}
/>

## Exposed Methods

### `start`

Starts the animation

```ueml copy /anim.start()/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeAnimation id="anim" from="0" to="1">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeAnimation>
</App>
```

<Playground
  name="Example: start api"
  app={`
    <App>
      <Button onClick="anim.start()">Start</Button>
      <FadeAnimation id="anim" from="0" to="1">
        <Image
          src="/resources/images/components/image/breakfast.jpg" width="500"/>
      </FadeAnimation>
    </App>
  `}
/>

### `stop`

Stops the animation

```ueml copy /anim.stop()/
<App>
  <Button onClick="anim.start()">Start</Button>
  <Button onClick="anim.stop()">Stop</Button>
  <FadeAnimation id="anim" from="0" to="1" duration="10000">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeAnimation>
</App>
```

<Playground
  name="Example: stop api"
  app={`
    <App>
      <Button onClick="anim.start()">Start</Button>
      <Button onClick="anim.stop()">Stop</Button>
      <FadeAnimation id="anim" from="0" to="1" duration="10000">
        <Image
          src="/resources/images/components/image/breakfast.jpg" width="500"/>
      </FadeAnimation>
    </App>
  `}
/>

## Styling

This component does not have any styles.
