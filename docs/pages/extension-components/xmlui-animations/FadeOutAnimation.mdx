import { Callout } from "nextra/components";

# FadeOutAnimation [#component-fadeoutanimation]

<Callout type="warning" emoji="📔">This component is in an **experimental** state; you can use it in your app. However, we may modify it, and it may even have breaking changes in the future.</Callout>

The `FadeOutAnimation` component represents an animation that fades out the content.

## Properties

### `animateWhenInView`

Indicates whether the animation should start when the component is in view

```ueml copy /animateWhenInView/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeOutAnimation id="anim"  animateWhenInView>
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeOutAnimation>
</App>
```

<Playground
  name="Example: animateWhenInView"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <FadeOutAnimation id="anim"  animateWhenInView>
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </FadeOutAnimation>
  </App>
  `}
/>

### `delay (default: 0)`

The delay before the animation starts in milliseconds

```ueml copy /delay="1000"/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeOutAnimation id="anim"  delay="1000">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeOutAnimation>
</App>
```

<Playground
  name="Example: delay"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <FadeOutAnimation id="anim"  delay="1000">
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </FadeOutAnimation>
  </App>
  `}
/>

### `duration`

The duration of the animation in milliseconds

```ueml copy /duration/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeOutAnimation id="anim"  duration="3000">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeOutAnimation>
</App>
```

<Playground
  name="Example: duration"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <FadeOutAnimation id="anim"  duration="3000">
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </FadeOutAnimation>
  </App>
  `}
/>

### `loop (default: false)`

Indicates whether the animation should loop

```ueml copy /loop/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeOutAnimation id="anim"  loop>
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeOutAnimation>
</App>
```

<Playground
  name="Example: loop"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <FadeOutAnimation id="anim"  loop>
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </FadeOutAnimation>
  </App>
  `}
/>

### `reverse (default: false)`

Indicates whether the animation should run in reverse

```ueml copy /reverse/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeOutAnimation id="anim"  reverse>
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeOutAnimation>
</App>
```

<Playground
  name="Example: reverse"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <FadeOutAnimation id="anim"  reverse>
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </FadeOutAnimation>
  </App>
  `}
/>

## Events

### `started`

Event fired when the animation starts

```ueml copy /onStarted="toast('Animation start')"/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeOutAnimation id="anim"  onStarted="toast('Animation start')">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeOutAnimation>
</App>
```

<Playground
  name="Example: started"
  app={`
    <App>
      <Button onClick="anim.start()">Start</Button>
      <FadeOutAnimation id="anim"  onStarted="toast('Animation start')">
        <Image
          src="/resources/images/components/image/breakfast.jpg" width="500"/>
      </FadeOutAnimation>
    </App>
  `}
/>

### `stopped`

Event fired when the animation stops

```ueml copy /onStopped="toast('Animation start')"/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeOutAnimation id="anim"  onStopped="toast('Animation end')">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeOutAnimation>
</App>
```

<Playground
  name="Example: stopped"
  app={`
    <App>
      <Button onClick="anim.start()">Start</Button>
      <FadeOutAnimation id="anim"  onStopped="toast('Animation end')">
        <Image
          src="/resources/images/components/image/breakfast.jpg" width="500"/>
      </FadeOutAnimation>
    </App>
  `}
/>

## Exposed Methods

### `start`

Starts the animation

```ueml copy /anim.start()/
<App>
  <Button onClick="anim.start()">Start</Button>
  <FadeOutAnimation id="anim" >
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeOutAnimation>
</App>
```

<Playground
  name="Example: start api"
  app={`
    <App>
      <Button onClick="anim.start()">Start</Button>
      <FadeOutAnimation id="anim" >
        <Image
          src="/resources/images/components/image/breakfast.jpg" width="500"/>
      </FadeOutAnimation>
    </App>
  `}
/>

### `stop`

Stops the animation

```ueml copy /anim.stop()/
<App>
  <Button onClick="anim.start()">Start</Button>
  <Button onClick="anim.stop()">Stop</Button>
  <FadeOutAnimation id="anim"  duration="10000">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </FadeOutAnimation>
</App>
```

<Playground
  name="Example: stop api"
  app={`
    <App>
      <Button onClick="anim.start()">Start</Button>
      <Button onClick="anim.stop()">Stop</Button>
      <FadeOutAnimation id="anim"  duration="10000">
        <Image
          src="/resources/images/components/image/breakfast.jpg" width="500"/>
      </FadeOutAnimation>
    </App>
  `}
/>

## Styling

This component does not have any styles.
