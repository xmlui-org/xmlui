import { Callout } from "nextra/components";

# ScaleAnimation [#component-scaleanimation]

<Callout type="warning" emoji="📔">This component is in an **experimental** state; you can use it in your app. However, we may modify it, and it may even have breaking changes in the future.</Callout>

The `ScaleAnimation` component represents an animation that scales the content.

## Properties

### `animateWhenInView`

Indicates whether the animation should start when the component is in view.

```ueml copy /animateWhenInView/
<App>
  <Button onClick="anim.start()">Start</Button>
  <ScaleAnimation id="anim" from="0" to="1" animateWhenInView>
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </ScaleAnimation>
</App>
```

<Playground
  name="Example: animateWhenInView"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <ScaleAnimation id="anim" from="0" to="1" animateWhenInView>
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </ScaleAnimation>
  </App>
  `}
/>

### `delay (default: 0)`

The delay before the animation starts in milliseconds

```ueml copy /delay="1000"/
<App>
  <Button onClick="anim.start()">Start</Button>
  <ScaleAnimation id="anim" from="0" to="1" delay="1000">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </ScaleAnimation>
</App>
```

<Playground
  name="Example: delay"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <ScaleAnimation id="anim" from="0" to="1" delay="1000">
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </ScaleAnimation>
  </App>
  `}
/>

### `duration`

The duration of the animation in milliseconds.

```ueml copy /duration/
<App>
  <Button onClick="anim.start()">Start</Button>
  <ScaleAnimation id="anim" from="0" to="1" duration="3000">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </ScaleAnimation>
</App>
```

<Playground
  name="Example: duration"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <ScaleAnimation id="anim" from="0" to="1" duration="3000">
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </ScaleAnimation>
  </App>
  `}
/>

### `from (default: 0)`

The initial scale of the content.

```ueml copy /from="0.5"/
<App>
  <Button onClick="anim.start()">Start</Button>
  <ScaleAnimation id="anim" from="0.5">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </ScaleAnimation>
</App>
```

<Playground
  name="Example: from"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <ScaleAnimation id="anim" from="0.5">
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </ScaleAnimation>
  </App>
  `}
/>

### `loop (default: false)`

Indicates whether the animation should loop

```ueml copy /loop/
<App>
  <Button onClick="anim.start()">Start</Button>
  <ScaleAnimation id="anim" from="0" to="1" loop>
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </ScaleAnimation>
</App>
```

<Playground
  name="Example: loop"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <ScaleAnimation id="anim" from="0" to="1" loop>
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </ScaleAnimation>
  </App>
  `}
/>

### `reverse (default: false)`

Indicates whether the animation should run in reverse

```ueml copy /reverse/
<App>
  <Button onClick="anim.start()">Start</Button>
  <ScaleAnimation id="anim" from="0" to="1" reverse>
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </ScaleAnimation>
</App>
```

<Playground
  name="Example: reverse"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <ScaleAnimation id="anim" from="0" to="1" reverse>
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </ScaleAnimation>
  </App>
  `}
/>

### `to (default: 1)`

The final scale of the content.

```ueml copy /to="0.5"/
<App>
  <Button onClick="anim.start()">Start</Button>
  <ScaleAnimation id="anim" to="0.5">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </ScaleAnimation>
</App>
```

<Playground
  name="Example: to"
  app={`
  <App>
    <Button onClick="anim.start()">Start</Button>
    <ScaleAnimation id="anim" to="0.5">
      <Image
        src="/resources/images/components/image/breakfast.jpg" width="500"/>
    </ScaleAnimation>
  </App>
  `}
/>

## Events

### `started`

Event fired when the animation starts

```ueml copy /onStarted="toast('Animation start')"/
<App>
  <Button onClick="anim.start()">Start</Button>
  <ScaleAnimation id="anim" from="0" to="1" onStarted="toast('Animation start')">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </ScaleAnimation>
</App>
```

<Playground
  name="Example: started"
  app={`
    <App>
      <Button onClick="anim.start()">Start</Button>
      <ScaleAnimation id="anim" from="0" to="1" onStarted="toast('Animation start')">
        <Image
          src="/resources/images/components/image/breakfast.jpg" width="500"/>
      </ScaleAnimation>
    </App>
  `}
/>

### `stopped`

Event fired when the animation stops

```ueml copy /onStopped="toast('Animation start')"/
<App>
  <Button onClick="anim.start()">Start</Button>
  <ScaleAnimation id="anim" from="0" to="1" onStopped="toast('Animation end')">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </ScaleAnimation>
</App>
```

<Playground
  name="Example: stopped"
  app={`
    <App>
      <Button onClick="anim.start()">Start</Button>
      <ScaleAnimation id="anim" from="0" to="1" onStopped="toast('Animation end')">
        <Image
          src="/resources/images/components/image/breakfast.jpg" width="500"/>
      </ScaleAnimation>
    </App>
  `}
/>

## Exposed Methods

### `start`

Starts the animation

```ueml copy /anim.start()/
<App>
  <Button onClick="anim.start()">Start</Button>
  <ScaleAnimation id="anim" from="0" to="1">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </ScaleAnimation>
</App>
```

<Playground
  name="Example: start api"
  app={`
    <App>
      <Button onClick="anim.start()">Start</Button>
      <ScaleAnimation id="anim" from="0" to="1">
        <Image
          src="/resources/images/components/image/breakfast.jpg" width="500"/>
      </ScaleAnimation>
    </App>
  `}
/>

### `stop`

Stops the animation

```ueml copy /anim.stop()/
<App>
  <Button onClick="anim.start()">Start</Button>
  <Button onClick="anim.stop()">Stop</Button>
  <ScaleAnimation id="anim" from="0" to="1" duration="10000">
    <Image
      src="/resources/images/components/image/breakfast.jpg" width="500"/>
  </ScaleAnimation>
</App>
```

<Playground
  name="Example: stop api"
  app={`
    <App>
      <Button onClick="anim.start()">Start</Button>
      <Button onClick="anim.stop()">Stop</Button>
      <ScaleAnimation id="anim" from="0" to="1" duration="10000">
        <Image
          src="/resources/images/components/image/breakfast.jpg" width="500"/>
      </ScaleAnimation>
    </App>
  `}
/>

## Styling

This component does not have any styles.
