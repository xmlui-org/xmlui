import type { LabelPosition } from "recharts/types/component/Label";

export const defaultProps = {
  showLabel: true,
  showLabelList: false,
  showLegend: false,
  labelListPosition: "inside" as LabelPosition,
  innerRadius: 0,
};
