%-PROP-START autoFocus

```ueml copy
<TextArea autoFocus="true" />
```

<Playground
  previewOnly
  name="Example: autoFocus"
  height={80}
  app={`<TextArea autoFocus="true" />`}
/>

%-PROP-END

%-PROP-START autoSize

> **Note**: If either `autoSize`, `maxRows` or `minRows` is set, the `rows` prop has no effect.

Write multiple lines in the `TextArea` in the demo below to see how it resizes automatically.

```ueml copy
<TextArea autoSize="true" />
```

<Playground
  previewOnly
  name="Example: autoSize"
  height={100}
  app={`<TextArea autoSize="true" />`}
/>

%-PROP-END

%-PROP-START enabled

```ueml copy
<TextArea enabled="false" />
```

<Playground
  previewOnly
  name="Example: enabled"
  app={`<TextArea enabled="false" />`}
  height={80}
/>

%-PROP-END

%-PROP-START enterSubmits

This optional boolean property toggles whether pressing the `Enter` key on the keyboard prompts the parent `Form` component to submit.
The default value is is `true`.

Press `Enter` after writing something in the `TextArea` in the demo below.
See [Using Forms](../learning/forms.mdx) for details.

```ueml copy
<Form onSubmit="window.alert(address.value)">
    <TextArea
      id="address"
      enterSubmits="true"
      initialValue="Suzy Queue, 4455 Landing Lange, APT 4, Louisville, KY 40018-1234"
    />
</Form>
```

<Playground
  previewOnly
  name="Example: enterSubmits"
  app={`<Form onSubmit="window.alert(address.value)">
    <TextArea
      id="address"
      enterSubmits="true"
      initialValue="Suzy Queue, 4455 Landing Lange, APT 4, Louisville, KY 40018-1234"
    />
</Form>`}
/>

%-PROP-END

%-PROP-START initialValue

The initial value displayed in the input field.

```ueml copy
<TextArea initialValue="Example text" />
```

<Playground
  previewOnly
  name="Example: initialValue"
  height={80}
  app={`<TextArea initialValue="Example text" />`}
/>

%-PROP-END

%-PROP-START maxRows

This optional number property maximizes the number of lines the `TextArea` can grow to.

> **Note**: If either `autoSize`, `maxRows` or `minRows` is set, the `rows` prop has no effect.

```ueml copy
<TextArea
  maxRows="3"
  initialValue="Lorem ipsum dolor sit amet,
  consectetur adipiscing elit,
  sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.
  Ut enim ad minim veniam,
  quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat."
/>
```

<Playground
  previewOnly
  name="Example: maxRows"
  app={`<TextArea
  maxRows="3"
  initialValue="Lorem ipsum dolor sit amet,
consectetur adipiscing elit,
sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.
Ut enim ad minim veniam,
quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat."
/>`}
  height={100}
/>

%-PROP-END

%-PROP-START minRows

This optional number property indicates the minimum number of lines the `TextArea` has and specifies its size accordingly.

> **Note**: If either `autoSize`, `maxRows` or `minRows` is set, the `rows` prop has no effect.

```ueml copy
<TextArea minRows="3" initialValue="Lorem ipsum dolor sit amet..." />
```

<Playground
  previewOnly
  name="Example: minRows"
  app={`<TextArea minRows="3" initialValue="Lorem ipsum dolor sit amet..." />`}
  height={100}
/>

%-PROP-END

%-PROP-START placeholder

```ueml copy
<TextArea placeholder="This is a placeholder" />
```

<Playground
  previewOnly
  name="Example: placeholder"
  height={80}
  app={`<TextArea placeholder="This is a placeholder" />`}
/>

%-PROP-END

%-PROP-START readOnly

```ueml copy /readOnly="{true}"/
<TextArea initialValue="Example text" readOnly="{true}" />
```

<Playground
  previewOnly
  name="Example: readOnly"
  height={80}
  app={`<TextArea initialValue="Example text" readOnly="{true}" />`}
/>

%-PROP-END

%-PROP-START resize

This optional property specifies in which dimensions can the `TextArea` be resized by the user.

| Value        | Description                   |
| :----------- | :---------------------------- |
|  undefined   | No resizing                   |
| `horizontal` | Can only resize horizontally  |
| `vertical`   | Can only resize vertically    |
| `both`       | Can resize in both dimensions |

If you allow resizing, the `TextArea` turns off automatic sizing.

When you allow vertical resizing, you can limit the sizable range according to `minRows` and `maxRows`. 

Drag the small resize indicators at the bottom right on each of the controls in the demo.

```ueml copy
<VStack gap="0.5rem">
    <TextArea resize="vertical" minRows="1" maxRows="8" />
    <TextArea resize="both" />
</VStack>
```

<Playground
  previewOnly
  name="Example: resize"
  app={`<VStack gap="0.5rem">
    <TextArea resize="vertical" minRows="1" maxRows="8" />
    <TextArea resize="both" />
</VStack>`}
  height={300}
/>

%-PROP-END

%-PROP-START rows

Specifies the number of lines or rows (and thus, its size) the component has.

> **Note**: If either `autoSize`, `maxRows` or `minRows` is set, the `rows` prop has no effect.

```ueml copy
<TextArea rows="10" />
```

<Playground
  previewOnly
  name="Example: rows"
  app={`<TextArea rows="10" />`}
  height={300}
/>

%-PROP-END

%-PROP-START validationStatus

This prop is used to visually indicate status changes reacting to form field validation.

| Value     | Description                                           |
| :-------- | :---------------------------------------------------- |
| `valid`   | Visual indicator for an input that is accepted        |
| `warning` | Visual indicator for an input that produced a warning |
| `error`   | Visual indicator for an input that produced an error  |

```ueml copy
<VStack gap="0.5rem">
  <TextArea />
  <TextArea validationStatus="valid" />
  <TextArea validationStatus="warning" />
  <TextArea validationStatus="error" />
</VStack>
```

<Playground
  previewOnly
  name="Example: validationStatus"
  height={300}
  app={`
  <VStack gap="0.5rem">
    <TextArea />
    <TextArea validationStatus="valid" />
    <TextArea validationStatus="warning" />
    <TextArea validationStatus="error" />
  </VStack>
  `}
/>

%-PROP-END

%-EVENT-START didChange

Write in the input field and see how the `Text` underneath it is updated in parallel.

```ueml copy {2}
<VStack gap="0.5rem" var.field="">
  <TextArea initialValue="{field}" onDidChange="(val) => field = val" />
  <Text initialValue="{field}" />
</VStack>
```

<Playground
  previewOnly
  name="Example: didChange"
  height={100}
  app={`
  <VStack gap="0.5rem" var.field="">
    <TextArea initialValue="{field}" onDidChange="(val) => field = val" />
    <Text value="{field}" />
  </VStack>
  `}
/>

%-EVENT-END

%-EVENT-START gotFocus

Clicking on the `TextArea` in the example demo changes the label text.
Note how clicking elsewhere resets the text to the original.

```ueml copy {3-4}
<TextArea
  initialValue="{focused === true ? 'I got focused!' : 'I lost focus...'}"
  onGotFocus="focused = true"
  onLostFocus="focused = false"
  var.focused="{false}"
/>
```

<Playground
  previewOnly
  name="Example: gotFocus/lostFocus"
  height={100}
  app={`
  <TextArea
    initialValue="{focused === true ? 'I got focused!' : 'I lost focus...'}"
    onGotFocus="focused = true"
    onLostFocus="focused = false"
    var.focused="{false}"
  />
  `}
/>

%-EVENT-END

%-API-START focus

```ueml copy {2-3}
<VStack gap="$space-0_5">
  <Button label="Trigger Focus" onClick="inputComponent.focus()" />
  <TextArea id="inputComponent" />
</VStack>
```

<Playground
  previewOnly
  name="Example: focus"
  height={120}
  app={`
  <VStack gap="$space-0_5">
    <Button label="Trigger Focus" onClick="inputComponent.focus()" />
    <TextArea id="inputComponent" />
  </VStack>
  `}
/>

%-API-END

%-API-START value

In the example below, typing in the `TextArea` will also display the length of the text typed into it above the field:

```ueml copy {2-3}
<HStack gap="$space-0_5">
  <Text value="TextArea content length: {inputComponent.value.length}" />
  <TextArea id="inputComponent" />
</HStack>
```

<Playground
  previewOnly
  name="Example: value"
  height={60}
  app={`
  <HStack gap="$space-0_5">
    <Text value="TextArea content length: {inputComponent.value.length}" />
    <TextArea id="inputComponent" />
  </HStack>
  `}
/>

%-API-END

%-API-START setValue

```ueml copy {10}
<VStack margin="$space-1" gap="$space-0_5" var.changes="">
  <TextArea 
    id="inputField" 
    readOnly="true" 
    onDidChange="changes++"
  />
  <HStack gap="$space-1">
    <Button 
      label="Check" 
      onClick="inputField.setValue('example ')"
    />
    <Text value="Number of changes: {changes}" />
  </HStack>
</VStack>
```

<Playground
  previewOnly
  name="Example: setValue"
  app={`
  <VStack margin="$space-1" gap="$space-0_5" var.changes="">
    <TextArea 
      id="inputField" 
      readOnly="true" 
      onDidChange="changes++"
    />
    <HStack gap="$space-1">
      <Button 
        label="Check" 
        onClick="inputField.setValue('example ')"
      />
      <Text value="Number of changes: {changes}" />
    </HStack>
  </VStack>
  `}
/>

%-API-END

%-STYLE-START

The `TextArea` component uses these theme variables.

These variables have 4 variants depending on the [validationStatus](#validationstatus) denoted by `|status|`.
If the status segment is omitted, the style will be applied to all validation statuses.

- `radius-TextArea-|status|`
- `thickness-border-TextArea-|status|`
- `style-border-TextArea-|status|`
- `font-size-TextArea-|status|`
- `color-placeholder-TextArea-|status|`

```json copy
"radius-TextArea-default": "10px",
"color-placeholder-TextArea-error": "crimson"
```

In addition to the validation statuses,
the variables below are associated with styles for `hover` and `focus` interaction states denoted by `|interact|` in their signature:

- `color-border-TextArea-|status|--|interact|`
- `color-bg-TextArea-|status|--|interact|`
- `shadow-TextArea-|status|--|interact|`
- `color-text-TextArea-|status|--|interact|`

```json copy
"color-bg-TextArea-default--hover": "gray",
"color-text-TextArea-error--focus": "lightcoral"
```

The following variables are only used in the `focus` state:

- `thickness-outline-TextArea-|status|--focus`
- `color-outline-TextArea-|status|--focus`
- `style-outline-TextArea-|status|--focus`
- `offset-outline-TextArea-|status|--focus`

These variables set the `disabled` state:

- `color-bg-TextArea--disabled`
- `color-text-TextArea--disabled`
- `color-border-TextArea--disabled`

### Example

```json copy
"radius-TextArea": "30px",
"color-placeholder-TextArea-default": "blue"
```

<Playground
  name="Example: styling"
  previewOnly
  height={200}
  defaultTheme="custom"
  themes={[
    {
      id: "custom",
      name: "Custom Theme",
      themeVars: {
        "radius-TextArea": "30px",
        "color-placeholder-TextArea-default": "blue"
      },
    },
  ]}
  app={`<TextArea placeholder="I am a placeholder" />`}
/>

To style all input controls not just this component, replace the `TextArea` segment with `Input`:

```
color-border-Input: "#0033FF"
```

`Input` affects the following controls:

- [`Checkbox`](./Checkbox.mdx)
- [`NumberBox`](./NumberBox.mdx)
- [`RadioGroupOption`](./RadioGroupOption.mdx)
- [`Select`](./Select.mdx)
- [`Switch`](./Switch.mdx)
- [`TextBox`](./TextBox.mdx)
- [`MultiSelect`](./MultiSelect.mdx)
- [`MultiCombobox`](./MultiCombobox.mdx)

See [custom themes](../learning/themes/overview.mdx) for styling details.

%-STYLE-END
