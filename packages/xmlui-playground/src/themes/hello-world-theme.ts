import type { ThemeDefinition } from "xmlui";

export const HelloWorldTheme: ThemeDefinition = {
  name: "Hello World Theme",
  id: "hello-world-theme",
  extends: ["xmlui"],
  themeVars: {
  },
};

export default HelloWorldTheme;
