# EmojiSelector

The `EmojiSelector` component provides users with a graphical interface to browse,
search and select emojis to insert into text fields, messages, or other forms of communication.

## Props

This component does not have any properties.

## Events

### `select`

This event is fired when the user selects an emoticon from this component.

```ueml copy
<HStack var.selected="" gap="1rem">
    <EmojiSelector onSelect="(emoji) => { selected = emoji }" />
    <Text value="Selected emoji: {selected}" />
</HStack>
```

<Playground
  name="Example: select event"
  previewOnly={true}
  height={380}
  app={`<HStack var.selected="" gap="1rem">
    <EmojiSelector onSelect="(emoji) => { selected = emoji }" />
    <Text value="Selected emoji: {selected}" />
</HStack>
  `}
/>

## Styling

This component does not have any styling options.
