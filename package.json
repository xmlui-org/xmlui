{
  "name": "xmlui-root",
  "version": "1.0.0",
  "description": "Root folder of XMLUI",
  "private": true,
  "workspaces": [
    "./xmlui",
    "./tests",
    "./docs",
    "./tools/create-app",
    "./tools/vscode",
    "./packages/*"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.29.4",
    "@typescript-eslint/eslint-plugin": "^8.15.0",
    "turbo": "^2.5.3"
  },
  "packageManager": "npm@10.9.2",
  "scripts": {
    "changeset:add": "changeset add",
    "changeset:version": "changeset version",
    "changeset:publish": "changeset publish",
    "build-xmlui": "turbo run build:xmlui-all --filter=xmlui",
    "test-xmlui": "turbo run build:xmlui-all test:xmlui-all --filter=xmlui --filter=xmlui-e2e",
    "test-xmlui-smoke": "turbo run build:xmlui-all test:xmlui-smoke --filter=xmlui --filter=xmlui-e2e",
    "publish-packages": "turbo run build:xmlui-all build:extension test:xmlui-all && npm run changeset:publish"
  }
}
