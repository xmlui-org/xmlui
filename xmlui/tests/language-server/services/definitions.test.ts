import { describe, it } from "vitest";
import { Project } from "../../../src/language-server/project";

describe("Goto Definition", () => {
  it("locates component def in another file", () => {});
});
