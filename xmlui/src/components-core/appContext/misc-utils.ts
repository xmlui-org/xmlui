import { capitalize, defaultTo } from "lodash-es";

import { formatFileSizeInBytes, getFileExtension, pluralize } from "@components-core/utils/misc";

export const miscellaneousUtils = {
  formatFileSizeInBytes,
  getFileExtension,
  capitalize,
  pluralize,
  defaultTo
};
