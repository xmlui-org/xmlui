import { capitalize, defaultTo } from "lodash-es";

import { findByField, pluralize, toHashObject } from "@components-core/utils/misc";

export const miscellaneousUtils = {
  capitalize,
  pluralize,
  defaultTo,
  toHashObject,
  findByField,
};
