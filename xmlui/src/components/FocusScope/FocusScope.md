# FocusScope

`FocusScope` keeps keyboard focus inside a temporary surface and restores focus when that surface closes.

```xmlui copy
<FocusScope>
  <Button label="Cancel" />
  <Button label="Save" />
</FocusScope>
```
