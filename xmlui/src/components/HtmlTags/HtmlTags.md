# HtmlTags

Deprecated compatibility wrappers for native HTML tags. They preserve the old
XMLUI authoring model where lowercase HTML tags such as `table`, `thead`, `tr`,
`th`, and `td` can appear in XMLUI markup and receive arbitrary native
attributes.
