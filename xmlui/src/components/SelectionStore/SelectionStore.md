`SelectionStore` is a deprecated non-visual mediator for selection-aware components.

```xmlui-pg copy display name="Example: using SelectionStore"
<SelectionStore id="selection">
  <Text>Selection-aware children can use this store.</Text>
</SelectionStore>
```

This rewrite slice provides the source-adjacent foundation and public APIs needed by later selection-aware components.
