import { EMPTY_OBJECT } from "../../components-core/constants";

export const defaultProps = {
  isRoot: false,
  applyIf: true,
  toastDuration: 5000,
  notificationPosition: "top-end" as const,
  themeVars: EMPTY_OBJECT,
  root: false,
};
