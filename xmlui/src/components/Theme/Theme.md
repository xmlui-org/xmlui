# Theme

`Theme` scopes theme variables to its children and can set the active tone for
that subtree.

This foundation slice keeps arbitrary theme-variable props and tone scoping.
Named theme selection, inline-style disabling, root style injection, and toast
configuration parity are deferred.
