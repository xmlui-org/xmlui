# Bookmark

`Bookmark` places an anchor in the view and exposes a `scrollIntoView()` API.

The table-of-contents registration path is deferred to the broader
TableOfContents/Bookmark compatibility closure. This foundation slice preserves
the anchor, children, metadata, and imperative scroll API.
