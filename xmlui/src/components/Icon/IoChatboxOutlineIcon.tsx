import Icon from "./svg/io-chatbox-outline.svg?react";
import type { IconBaseProps } from "./IconNative";

export const IoChatboxOutlineIcon = (props: IconBaseProps) => <Icon fill="currentColor" stroke="currentColor" strokeWidth={0} {...props}/>;
