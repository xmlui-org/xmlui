import Icon from "./svg/gr-document-config.svg?react";
import type { IconBaseProps } from "./IconNative";

export const GrDocumentConfigIcon = (props: IconBaseProps) => <Icon fill="currentColor" stroke="currentColor" strokeWidth={0} {...props}/>;
