import Icon from "./svg/io-swap-vertical.svg?react";
import type { IconBaseProps } from "./IconNative";

export const IoSwapVerticalIcon = (props: IconBaseProps) => <Icon fill="currentColor" stroke="currentColor" strokeWidth={0} {...props}/>;
