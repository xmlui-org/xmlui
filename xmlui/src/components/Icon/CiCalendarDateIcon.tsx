import Icon from "./svg/ci-calendar-date.svg?react";
import type { IconBaseProps } from "./IconNative";

export const CiCalendarDateIcon = (props: IconBaseProps) => <Icon fill="currentColor" stroke="currentColor" strokeWidth={0} {...props}/>;
