import Icon from "./svg/md-outline-delete-forever.svg?react";
import type { IconBaseProps } from "./IconNative";

export const TrashIcon = (props: IconBaseProps) => <Icon fill="currentColor" stroke="currentColor" strokeWidth={0} {...props}/>;
