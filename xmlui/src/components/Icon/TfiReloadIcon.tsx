import Icon from "./svg/tfi-reload.svg?react";
import type { IconBaseProps } from "./IconNative";

export const TfiReloadIcon = (props: IconBaseProps) => <Icon fill="currentColor" stroke="currentColor" strokeWidth={0} {...props}/>;
