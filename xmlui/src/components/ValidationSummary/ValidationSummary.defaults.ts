import { EMPTY_ARRAY, EMPTY_OBJECT } from "../../components-core/constants";

export const defaultProps = {
  fieldValidationResults: EMPTY_OBJECT as Record<string, any>,
  generalValidationResults: EMPTY_ARRAY as any[],
};
