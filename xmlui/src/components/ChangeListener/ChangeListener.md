# ChangeListener

`ChangeListener` is a non-visual compatibility component that watches a value
and runs `onDidChange` after the value changes.

This rewrite slice implements `listenTo`, `listenToSources`, debounce, and
throttle foundation behavior.
