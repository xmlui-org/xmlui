%-DESC-START

<Callout type="info" emoji="📔">
You can learn more details about using this component [here](../learning/using-components/app-component).
</Callout>

%-DESC-END

%-PROP-START logoTemplate

This property defines the template to use for the logo.
With this property, you can construct your custom logo instead of using a single image.

```ueml copy {2-8}
<App>
  <AppHeader>
    <property name="logoTemplate">
      <H3>
        <Icon name="drive" />
        DriveDiag
      </H3>
    </property>
  </AppHeader>
  <NavPanel>
    <NavLink label="Home" to="/" icon="home"/>
    <NavLink label="Page 1" to="/page1"/>
  </NavPanel>
  <Pages defaultRoute="/">
    <Page url="/">
      <Text value="Home" />
    </Page>
    <Page url="/page1">
      <Text value="Page 1" />
    </Page>
  </Pages>
</App>
```

<Playground
  name="Example: logoTemplate"
  horizontal
  height={150}
  app={`
  <App>
    <AppHeader>
    <property name="logoTemplate">
        <H3>
          <Icon name="drive" />
          DriveDiag
        </H3>
    </property>
    </AppHeader>
    <NavPanel>
      <NavLink label="Home" to="/" icon="home"/>
      <NavLink label="Page 1" to="/page1"/>
    </NavPanel>
    <Pages defaultRoute="/">
      <Page url="/">
        <Text value="Home" />
      </Page>
      <Page url="/page1">
        <Text value="Page 1" />
      </Page>
    </Pages>
  </App>
  `}
/>

%-PROP-END

%-PROP-START profileMenuTemplate

This property makes the profile menu slot of the `AppHeader` component customizable.
It accepts component definitions.

```ueml copy {3-9}
<App>
  <AppHeader>
    <property name="profileMenuTemplate">
      <DropdownMenu>
        <property name="triggerTemplate">
          <Avatar name="Joe" size="xs" borderRadius="50%"/>
        </property>
      </DropdownMenu>
    </property>
  </AppHeader>
</App>
```

<Playground
  name="Example: profileMenuTemplate"
  horizontal
  height={150}
  app={`
  <App>
    <AppHeader>
      <property name="profileMenuTemplate">
        <DropdownMenu>
          <property name="triggerTemplate">
            <Avatar name="Joe" size="xs" borderRadius="50%"/>
          </property>
        </DropdownMenu>
      </property>
    </AppHeader>
  </App>
  `}
/>

%-PROP-END

%-STYLE-START

The `AppHeader` component uses these theme variables to customize its appearance:

- `color-bg-AppHeader`: sets the background color of the header
- `padding` (including all `padding` variants, such as `paddingLeft` and others)
- `color-border` (including all `color-border` variants, such as `borderLeftColor` and others)
- `style-border` (including all `style-border` variants, such as `borderLeftStyle` and others)
- `thickness-border` (including all `thickness-border` variants, such as `borderLeftWidth` and others)

The `AppHeader` component allows these theme variables to customize the logo within the header:

- `padding-logo-AppHeader` (including all `padding` variants, such as `paddingLeft-logo-AppHeader` and others)
- `width-logo-AppHeader`: sets the width of the displayed logo

%-STYLE-END
