# AppState

`AppState` is a non-visual compatibility component that exposes shared bucket
state through an id-based API.

This rewrite slice implements bucket sharing, initial value merge, update,
list helpers, and the `didUpdate` event.
