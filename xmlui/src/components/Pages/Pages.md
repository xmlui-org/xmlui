# Pages

`Pages` coordinates route matching for child `Page` components.

This foundation slice preserves static and parameterized route matching,
fallback navigation, and route/query context variables. Advanced guards,
canonical URL handling, search indexing, scroll restoration, and app-shell
navigation integration are deferred.
