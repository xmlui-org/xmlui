# NestedApp

`NestedApp` renders another XMLUI app from the source text in its `app`
property.

This foundation slice supports compiling and rendering nested source with an
isolated runtime state store. Shadow DOM isolation, playground framing,
split-view rendering, emulated APIs, component injection, and full theme/tone
bridging remain deferred.
