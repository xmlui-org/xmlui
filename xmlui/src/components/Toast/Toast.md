# Toast

`Toast` exposes imperative `show`, `success`, `error`, and `loading` APIs.

The original template rendering contract is only partially represented in this
foundation slice. Template-specific toast body rendering is deferred until the
old toast host behavior is fully migrated.
