# Table

`Table` presents structured data for viewing, sorting, and selection.

This foundation migration supports static `Column` children, row rendering,
basic sorting, basic row selection, and empty/loading output. The copied old
`Table` E2E suite remains the compatibility target for the complete table
engine.
