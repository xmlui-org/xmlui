# WebSocket

`WebSocket` manages a browser `WebSocket` connection declaratively.

This rewrite slice implements open, message, error, close, enabled state,
optional reconnect, JSON message parsing, and cleanup.
