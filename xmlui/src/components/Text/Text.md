# Text

Displays textual information. This migrated slice preserves the old value,
children, variant element, context-menu, overflow, and theme-variable contracts
needed by the primitive component wave.
