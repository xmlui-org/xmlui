# Queue

`Queue` is a non-visual component that processes enqueued items sequentially.

This rewrite slice implements FIFO enqueueing, processing, completion, removal,
queue state APIs, and queue context variables.
