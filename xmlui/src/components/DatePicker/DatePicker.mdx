%-API-START setValue

```ueml copy {3, 9, 12}
<VStack gap="0.5rem">
  <DatePicker
    id="picker"
    readOnly="true"
  />
  <HStack gap="0.5rem">
    <Button 
      label="Add Text" 
      onClick="picker.setValue('05/25/2024')" />
    <Button 
      label="Remove Text" 
      onClick="picker.setValue('')" />
  </HStack>
</VStack>
```

<Playground
  previewOnly
  name="Example: value and setValue"
  app={`
  <VStack gap="0.5rem">
    <DatePicker
      id="picker"
      readOnly="true"
    />
    <HStack gap="0.5rem">
      <Button 
        label="Add Text" 
        onClick="picker.setValue('05/25/2024')" />
      <Button 
        label="Remove Text" 
        onClick="picker.setValue('')" />
    </HStack>
  </VStack>`}
/>

%-API-END

%-PROP-START initialValue

```ueml copy
<DatePicker initialValue="05/25/2024" />
```

<Playground
  previewOnly
  height={60}
  name="Example: initialValue"
  app={`<DatePicker initialValue="05/25/2024" />`}
/>

%-PROP-END

%-PROP-START placeholder

```ueml copy
<DatePicker placeholder="This is a placeholder" />
```

<Playground
  previewOnly
  height={60}
  name="Example: placeholder"
  app={`<DatePicker placeholder="This is a placeholder" />`}
/>

%-PROP-END

%-PROP-START enabled

```ueml copy
<DatePicker enabled="false" />
```

<Playground
  previewOnly
  height={60}
  name="Example: enabled"
  app={`<DatePicker enabled="false" />`}
/>

%-PROP-END

%-PROP-START validationStatus

| Value     | Description                                           |
| :-------- | :---------------------------------------------------- |
| `valid`   | Visual indicator for an input that is accepted        |
| `warning` | Visual indicator for an input that produced a warning |
| `error`   | Visual indicator for an input that produced an error  |

```ueml copy
<VStack gap="0.5rem">
  <DatePicker />
  <DatePicker validationStatus="valid" />
  <DatePicker validationStatus="warning" />
  <DatePicker validationStatus="error" />
</VStack>
```

<Playground
  previewOnly
  name="Example: validationStatus"
  height={200}
  app={`
  <VStack gap="0.5rem">
    <DatePicker />
    <DatePicker validationStatus="valid" />
    <DatePicker validationStatus="warning" />
    <DatePicker validationStatus="error" />
  </VStack>`}
/>

%-PROP-END

%-PROP-START mode

```ueml copy {2-3}
<VStack>
  <DatePicker mode="single" />
  <DatePicker mode="range" />
</VStack>
```

<Playground
  previewOnly
  height={120}
  name="Example: mode"
  app={`
  <VStack>
    <DatePicker mode="single" />
    <DatePicker mode="range" />
  </VStack>`}
/>

%-PROP-END

%-PROP-START dateFormat

Formats handle years (`y`), months (`m` or `M`), days of the month (`d`).
Providing multiple placeholder letters changes the display of the date.

The table below shows the available date formats:

| Format     | Example    |
| :--------- | :--------- |
| MM/dd/yyyy | 05/25/2024 |
| MM-dd-yyyy | 05-25-2024 |
| yyyy/MM/dd | 2024/05/25 |
| yyyy-MM-dd | 2024-05-25 |
| dd/MM/yyyy | 25/05/2024 |
| dd-MM-yyyy | 25-05-2024 |
| yyyyMMdd   | 20240525   |
| MMddyyyy   | 05252024   |

```ueml copy /dateFormat="dd-MM-yyyy"/
<DatePicker dateFormat="dd-MM-yyyy" initialValue="05/25/2024" />
```

<Playground
  previewOnly
  height={60}
  name="Example: dateFormat"
  app={`<DatePicker dateFormat="dd-MM-yyyy" initialValue="05/25/2024" />`}
/>

%-PROP-END

%-PROP-START showWeekNumber

```ueml copy
<DatePicker showWeekNumber="true" />
```

<Playground
  previewOnly
  height={60}
  name="Example: showWeekNumber"
  app={`<DatePicker showWeekNumber="true" />`}
/>

%-PROP-END

%-PROP-START weekStartsOn

| Day       | Number |
| :-------- | :----- |
| Sunday    | 0      |
| Monday    | 1      |
| Tuesday   | 2      |
| Wednesday | 3      |
| Thursday  | 4      |
| Friday    | 5      |
| Saturday  | 6      |

```ueml copy
<DatePicker weekStartsOn="1" />
```

<Playground
  previewOnly
  name="Example: weekStartsOn"
  height={60}
  app={`<DatePicker weekStartsOn="1" />`}
/>

%-PROP-END

%-PROP-START fromDate

```ueml copy
<DatePicker fromDate="05/24/2024" />
```

<Playground
  previewOnly
  height={60}
  name="Example: fromDate"
  app={`<DatePicker fromDate="05/24/2024" />`}
/>

%-PROP-END

%-PROP-START toDate

```ueml copy
<DatePicker toDate="05/26/2024" />
```

<Playground
  previewOnly
  height={60}
  name="Example: toDate"
  app={`<DatePicker toDate="05/26/2024" />`}
/>

%-PROP-END

%-PROP-START disabledDates

```ueml copy
<DatePicker disabledDates="{['05/26/2024', '05/27/2024']}" initialValue="05/25/2024" />
```

<Playground
  previewOnly
  height={60}
  name="Example: disabledDates"
  app={`<DatePicker disabledDates="{['05/26/2024', '05/27/2024']}" initialValue="05/25/2024" />`}
/>

%-PROP-END

%-EVENT-START didChange

Write in the input field and see how the `Text` underneath it is updated in parallel.

```ueml copy {2}
<VStack gap="0.5rem" var.field="">
  <DatePicker initialValue="{field}" onDidChange="(val) => field = val" />
  <Text value="{field}" />
</VStack>
```

<Playground
  previewOnly
  height={120}
  name="Example: didChange"
  app={`
  <VStack gap="0.5rem" var.field="">
    <DatePicker initialValue="{field}" onDidChange="(val) => field = val" />
    <Text value="{field}" />
  </VStack>`}
/>

%-EVENT-END

%-EVENT-START gotFocus

Clicking on the `DatePicker` in the example demo changes the label text.
Note how clicking elsewhere resets the text to the original.

```ueml copy {4-5}
<VStack gap="0.5rem" var.isFocused="false">
  <Text value="{isFocused === true ? 'DatePicker focused' : 'DatePicker lost focus'}" />
  <DatePicker
    onGotFocus="isFocused = true"
    onLostFocus="isFocused = false"
  />
</VStack>
```

<Playground
  previewOnly
  height={120}
  name="Example: gotFocus/lostFocus"
  app={`
  <VStack gap="0.5rem" var.isFocused="false">
    <Text value="{isFocused === true ? 'DatePicker focused' : 'DatePicker lost focus'}" />
    <DatePicker
      onGotFocus="isFocused = true"
      onLostFocus="isFocused = false"
    />
  </VStack>`}
/>

%-EVENT-END

%-STYLE-START

The `DatePicker` component provides these theme variables to customize its appearance:

- `radius-DatePicker`
- `color-text-DatePicker`
- `color-bg-DatePicker--disabled`
- `thickness-border-DatePicker`
- `style-border-DatePicker`
- `color-border-DatePicker--disabled`
- `color-bg-DatePicker--disabled`
- `color-text-DatePicker--disabled`
- `color-text-value-DatePicker`
- `color-border-DatePicker-default`
- `color-border-DatePicker-error`
- `color-border-DatePicker-warning`
- `color-border-DatePicker-success`
- `color-placeholder-DatePicker`
- `color-bg-menu-DatePicker`
- `shadow-menu-DatePicker`
- `radius-menu-DatePicker`
- `color-bg-item-DatePicker--hover`
- `color-bg-item-DatePicker--active`

```json
{
  "radius-DatePicker": "30px",
  "thickness-border-DatePicker": "4px",
  "radius-menu-DatePicker": "30px",
  "color-text-DatePicker": "black",
  "color-bg-menu-DatePicker": "#f5f5dc"
}
```

<Playground
  name="Example: styling"
  previewOnly
  height={60}
  defaultTheme="custom"
  themes={[
    {
      id: "custom",
      name: "Custom Theme",
      themeVars: {
        "radius-DatePicker": "30px",
        "thickness-border-DatePicker": "4px",
        "radius-menu-DatePicker": "30px",
        "color-text-DatePicker": "black",
        "color-bg-menu-DatePicker": "#f5f5dc"
      },
    },
  ]}
  app={`<DatePicker />`}
/>

To style all input controls not just this component, use `Input` instead of the `DatePicker` segment:

```
color-text-Input: "#0033FF"
```

`Input` affects the following controls:

- [`Checkbox`](./Checkbox.mdx)
- [`NumberBox`](./NumberBox.mdx)
- [`RadioGroupOption`](./RadioGroupOption.mdx)
- [`Select`](./Select.mdx)
- [`Switch`](./Switch.mdx)
- [`TextArea`](./TextArea.mdx)

See [custom themes](../learning/themes/overview.mdx) for styling details.

%-STYLE-END
