# Timer

`Timer` is a non-visual component that fires `onTick` at a configured cadence.

This rewrite slice implements enabled/disabled state, interval, initial delay,
once mode, pause/resume, and overlapping tick prevention.
