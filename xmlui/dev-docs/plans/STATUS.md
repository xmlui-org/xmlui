# Managed-React Implementation Status

This document tracks the live status of every dimension from the
[Managed React assessment](./managed-react.md). Update it whenever
a plan phase ships: change the Status cell, fill in the Date, and add
a one-line note.

**Status values**

| Symbol | Meaning |
|---|---|
| ✅ | Closed — fully implemented and shipped |
| 🔄 | In progress |
| ⬜ | Not started |

> **Audit reconciliation (2026-05-19).** Several rows below were
> corrected against the codebase as part of the Wave-8 open-work
> audit. Rows #05, #07, #11 previously over- or under-stated their
> state and have been rewritten. Row #14's "Remaining" was
> strengthened to flag a half-open security boundary. The full
> per-plan inventory and Wave-8 readiness matrix live in
> [`00-master-plan.md` Appendix A](./00-master-plan.md#appendix-a--open-work-inventory-audit-dated-2026-05-19).
> Trust this STATUS row for the one-line summary; trust Appendix A
> for the exhaustive outstanding-step list.

---

## Closed dimensions

| Dimension | Status | Date | Notes |
|---|---|---|---|
| Code injection (`eval`, `Function`, `WebAssembly`, `debugger`) | ✅ | 2026-04 | Custom AST interpreter; all four families banned in `bannedFunctions.ts`; `debugger` rejected at parse time |
| XSS in default rendering | ✅ | 2026-04 | React JSX escaping + DOM-mutation ban via `bannedMembers.ts`; Markdown sanitization (rehype-raw) is a known residual gap outside the plan scope |
| HTTP centralisation + CSRF + origin allowlist | ✅ | 2026-04 | `App.fetch` gate via `RestApiProxy`; raw `fetch`/`XHR`/`WebSocket`/`EventSource`/`sendBeacon` banned |
| Fetch lifecycle | ✅ | 2026-04 | React Query + `AbortController`; stale requests cancelled on key change / unmount |
| Observability substrate | ✅ | 2026-04 | `pushXsLog` circular buffer + Inspector overlay; all trace `kind` types registered |
| DOM-API isolation | ✅ | 2026-04 | Property-access guard (`isBannedMember`) + 99-entry denylist + sanctioned `App.*` / `Log.*` / `Clipboard.*` / `<WebSocket>` / `<EventSource>` replacements. `console` remains in the denylist but is **allowed by default** via `appGlobals.allowConsole` (default `true`); set to `false` to restore sandbox-warn/throw behavior. |
| Type contracts | ✅ | 2026-05 | Metadata contracts verified through LSP diagnostics, Vite plugin warn/strict mode, runtime expression-value diagnostics, `check:metadata` TypeScript↔metadata drift guard, docs, and default-on `strictTypeContracts` with explicit `false` warn-only migration mode |
| Reactive cycle detection | ✅ | 2026-05 | Static/runtime reactive graph analyzer covers vars, functions, and loaders; runtime emits `reactive-cycle` traces with default-on strict escalation; LSP diagnostics include per-node related locations; Vite checks per-file and aggregate `buildEnd` graphs with warn/strict modes; docs and website page shipped |
| Enforced accessibility | ✅ | 2026-05 | Accessibility diagnostics are wired through LSP and Vite; linter covers accessible names, icon-only buttons, modal titles, form labels, duplicate landmarks, redundant roles, and missing skip links; `<SkipLink>`, `<FocusScope>`, and `<LiveRegion>` primitives shipped; toasts/errors announce through the global live region; theme contrast checker runs in dev/strict mode; `automationId` renders stable `data-automation-id`; docs and website page shipped. `strictAccessibility` remains opt-in until the next major default flip. |
| Managed lifecycle vocabulary (#04) | ✅ | 2026-05 | `components-core/lifecycle/` module: `LifecycleViolationError`, `createLifecycleDispatcher()`, `fireBeforeDispose()`; `reportLifecycleEvent`/`reportLifecycleViolation`; `kind:"lifecycle"` on `XsLogEntry`; `strictLifecycle` (default `true`, set `false` for warn-only) + `disposeTimeoutMs` (default `250`) on `appGlobals`; universal `onMount`/`onUnmount` on every component via `ComponentAdapter`; async violation reporting; per-component `onError` (`source:"mount"|"unmount"|"beforeDispose"|"action"`) suppresses global toast; `<Lifecycle>` non-visual component with `keyValue` re-arming; `onBeforeDispose` on containers with budget race. Docs: end-user page `/docs/managed-react/managed-lifecycle-vocabulary`, dev-guide chapter `28-lifecycle.md`, `.ai/xmlui/lifecycle.md`, `managed-react.md` scorecard ✅ Symmetric, `AGENTS.md` doc-map entry. |
| Structured exception model (#07) | ✅ | 2026-05 | `components-core/errors/` module: `AppError` class (`code`/`category`/`retryable`/`correlationId`/`data`/`toJSON()`/`from()`), `categorizeHttpStatus()`, `executeWithPolicy()` engine (fixed/linear/exponential backoff, jitter, circuit breaker, `parseRetryAfter()`), `RetryPolicyContext` bridge, diagnostics scaffolding; `kind:"errors"` registered on `XsLogEntry`. Normalisation sites wired: `signError` (handler/lifecycle), `LoaderComponent` (loader payload now `AppError`), `ErrorBoundary.componentDidCatch` (render), `RestApiProxy` (HTTP status → `ErrorCategory`, `X-Correlation-Id` extraction, `Retry-After` → `data.retryAfterMs`). App-level surface: `<App onError>` event, `App.errors` FIFO buffer (cap = `appGlobals.errorBufferSize`, default 50), Inspector "Errors" tab. Declarative components: `<RetryPolicy>` (attempts/backoff/delayMs/jitter/onlyCategories/timeoutMs/honourRetryAfter/circuitBreaker; cancellation-aware via `$cancel`) and `<Fallback>` (errorTemplate/loadingTemplate, `$error` context var). **`appGlobals.strictErrors` default flipped to `true` (W8-1)** — non-`AppError` throws emit a `kind:"errors"` warn; set `false` for migration mode. `appGlobals.errorCorrelationIdHeader` (default `"X-Correlation-Id"`). Docs: end-user page `/docs/managed-react/structured-exception-model`, dev-guide chapter `30-errors.md`, `.ai/xmlui/errors.md`, `managed-react.md` scorecard ✅ Structured, `AGENTS.md` doc-map entry. Unit tests in `tests/components-core/errors/`. |
| Defended routing (#10) | ✅ | 2026-05 | `components-core/routing/` module: constraint compiler/coercer, canonicaliser, guard dispatcher, diagnostics. `<Page url>` constraints (`int`/`number`/`uuid`/`slug`/`bool`/`enum`/`length`/`regex`) plus custom validators via forms validator registry (`App.registerValidator`). `<Page queryParams>` for query-string constraints. `<Page guard>` runs after route/query validation. Pop-state guard via `appGlobals.guardOnPopState` (default `true`). Opt-in same-origin anchor/form interception via `appGlobals.interceptExternalNavigation`. URL canonicalisation props on `<App>` (`urlCase`/`urlTrailingSlash`/`urlQueryParamOrder`/`nonCanonicalUrl`). **`appGlobals.strictRouting` default flipped to `true`** — routing diagnostics escalate to errors and `nonCanonicalUrl` defaults to `"redirect"`; set `false` for legacy warn-only behaviour. Docs: dev-guide chapter `13-routing.md`, `.ai/xmlui/routing.md`, `standalone.ts` JSDoc, user-facing page `/docs/managed-react/defended-routing`, `managed-react.md` scorecard ✅ Defended, `AGENTS.md` doc-map entry. Unit tests in `tests/components-core/routing/` (18 tests). |
| Build-validation analyzers (#13) | ✅ | 2026-06 | `components-core/analyzer/` module: rule registry, walker (auto-parses markup via `xmlUiMarkupToComponent` when no `markupAst` is supplied — first time markup rules fire across CLI / Vite / LSP without callers pre-parsing), suppression directives, `BuildDiagnostic` taxonomy. Rules shipped: `id-unknown-component`, `id-unknown-prop`, `id-unknown-event`, `id-unknown-method`, `expr-dead-conditional`, `expr-handler-no-value`, `expr-unbound-identifier`, `expr-unused-var`, `id-undefined-component-ref`, `id-undefined-form-ref`, `determinism-*`. `id-unknown-slot` registered as documented no-op (requires `ComponentMetadata.slots` field — out of plan scope). Shared infrastructure: `_ast-utils.ts` (lazy expression parsing, `collectIdentifierRefs`, `collectRootedChains`, `collectDeclaredNames`, `collectUidMap`, `iterComponentExpressions`), `_reserved-identifiers.ts` (framework context vars `$event`/`$cancel`/etc., global namespaces `App`/`Math`/`JSON`, XMLUI globals `navigate`/`toast`/…). Surfaces: LSP `diagnostic.ts`, Vite plugin `analyze: "off"\|"warn"\|"strict"`, `xmlui check [dir]` CLI (GNU + JSON formats, rule include/exclude filters). Templates: `check` script + `xmlui.config.json` + `.github/workflows/check.yml` in create-app templates. Docs: user-facing page `/docs/managed-react/build-validation-analyzers`. 102 analyzer unit tests under `tests/components-core/analyzer/`. |
| i18n foundations (#11) | ✅ | 2026-06 | `components-core/i18n/` module: `locale-resolver.ts`, `bundle-store.ts`, `translate.ts`, `icu.ts` (`@formatjs/intl-messageformat`), `formatters.ts`, `builtin-bundles/xmlui-en.ts`. `<App locale\|localeBundles\|direction>` props wired in `AppContent.tsx` + `AppReact.tsx`; `resolveLocale()` honours app-prop > user-override > persisted (`localePersistKey`) > `navigator.languages[]` > `defaultLocale`. Public `AppContext.App` namespace: `translate`/`t`, `setLocale`, `setAppDirection`, `locale`, `localeSource`, `availableLocales`, `direction` (reactive — respects explicit prop and auto-RTL), `isRtlLocale`, `registerLocaleBundle`/`registerLocaleBundles`/`reloadLocale`, `formatNumber`/`formatCurrency`/`formatList`/`formatRelativeTime`/`compare`/`pluralRules`. `<I18n key>` component with slot-based markup (`components/I18n/`). Built-in framework strings extracted to `xmlui.*` namespace; `SelectReact`, `DrawerReact`, `ValidationWrapper` route through `App.translate`. Full SCSS logical-properties migration + `scripts/lint-physical-css.ts` guard. Tests: 33 unit (`tests/components-core/i18n/`) + 18 E2E (`tests-e2e/i18n/`: locale-switch × 5, rtl-layout × 13). Docs: user-facing page `/docs/managed-react/i18n-foundations`; `managed-react.md` §11 resolved; `AGENTS.md` doc-map updated. `appGlobals.strictI18n` default flip reserved for next major (Phase 6.2 / W8). |
| Cooperative concurrency (#06) | ✅ | 2026-05 | `components-core/concurrency/` module shipped: `CancellationToken` (`aborted`/`reason`/`throwIfAborted`/`onAbort`/`signal`), `HandlerCancelledError`, `HandlerPolicy` (`parallel`/`single-flight`/`queue`/`drop-while-running`), real `HandlerCoordinator` with module-level singleton + per-`(componentUid, eventName)` running slot, `runWithTimeout()`, `createTransactionalBuffer` + `detectSnapshotConflict`. `kind:"concurrency"` registered on `XsLogEntry` with codes `concurrency-handler-cancelled`/`-superseded`/`-dropped`/`-timeout`/`-transactional-conflict`. Dispatcher integration: `event-handlers.ts runCodeAsync` injects a fresh `$cancel` token into every async handler (aborted on supersede/timeout/unmount/user), threads `coordinator.enter`/`exit`, races against `runWithTimeout` using per-handler `handlerTimeoutMs` or ambient `appGlobals.defaultHandlerTimeoutMs` (default `30000`; `<=0` disables), and replays/discards the transactional write buffer. `HandlerCancelledError` is swallowed so cancellation never surfaces on `onError`. `LookupActionOptions` carries `handlerPolicy`/`handlerTimeoutMs`/`transactional`; `ComponentAdapter.memoedLookupEventHandler` honours per-event overrides (`handlerPolicy:<event>` wins). `<Button busyOnClick>` is the documented `single-flight + $busy` shorthand. **Phase 1 Step 1.2 done (2026-05):** `App.cancel(componentUid?, eventName?)` global wired through `appCancel()` in `appContext/app-utils.ts` and registered on `App.*` in `AppContent.tsx`; routes through `getDefaultHandlerCoordinator().abortRunning(..., "user")` and emits a `concurrency-handler-cancelled` info trace. Phase 1 Step 1.3 verified — `$cancel.reason` is set correctly from every abort source (`user`/`supersede`/`timeout`/`unmount`). **`appGlobals.strictConcurrency` reader site landed (2026-05):** when `true`, the timeout `onTimeout` callback in `event-handlers.ts` escalates `concurrency-handler-timeout` from `warn` to `error`, emits `console.error`, and routes the timeout through `appContext.signError`; cancellation/supersession/drop remain info-level by design. Default stays `false` — the flip to `true` is reserved for the next major (plan §5.2). Docs: dev-guide chapter `29-concurrency.md`, `.ai/xmlui/concurrency.md`, end-user page `/docs/managed-react/cooperative-concurrency`, `managed-react.md` scorecard ✅ Predictable/coordinated, `AGENTS.md` doc-map entry. Unit tests in `tests/components-core/concurrency/` (38 + 4 new in `app-cancel.test.ts`).
| Sealed theming sandbox (#08) | ✅ | 2026-05-31 | `components-core/theming/validator/` module: `ThemeDiagnosticCode`/`ThemeDiagnostic` taxonomy (`invalid-theme-value`/`unknown-theme-variable`/`raw-css-in-prop`/`important-blocked`/`url-in-style`/`position-fixed-blocked`); `validateTheme()` wired into `useCompiledTheme` in `ThemeProvider.tsx`; `validateInlineStyle()` + `validateStyleString()` wired into `valueExtractor.ts` (`asLayoutProp`/`asStyleProp`); 7 new value-type rules (`duration`/`easing`/`shadow`/`border`/`fontFamily`/`fontWeight`/`lineHeight`) sharing the type-contract coercion table; `kind:"theming"` registered on `XsLogEntry`. `appGlobals.strictTheming` default flipped to `true` (all tests pass); `allowInlineRawCss` (default `true`), `maxZIndex` (default `9999`). User-facing docs page `/docs/managed-react/sealed-theming-sandbox` shipped. 72 unit tests. Deferred to a future sweep: component-metadata `valueType` annotation on built-in `themeVars` blocks; dev-guide chapter `31-theming-sandbox.md`. |
| Forms validation discipline (#09) | ✅ | 2026-06 | Docs shipped (2026-06): user-facing page `/docs/managed-react/forms-validation-discipline` + NavLink + Page route in `website/src/Main.xmlui`; covers built-in registry, custom `App.registerValidator`, `<FormValidator>` cross-field, RFC 7807/Spring/Laravel server-error mapping, `submitPolicy`/`Form.cancel`, `kind:"forms"` diagnostic table, `strictForms` flag. W5-1 done: validator registry (`components-core/forms/`) with `registerValidator`/`lookupValidator`/`hasValidator`/`runValidator`, sync + async support, parameterised + multi-validator on `<FormItem>`; `appGlobals.strictForms` (default `false`) drives diagnostic severity; `kind:"forms"` registered with codes `unknown-validator`/`duplicate-validator`/`server-error-unmapped`/`submit-while-busy`/`csrf-token-missing`/`validator-throw`/`deprecated-alias`; 9 builtins (`required`/`email`/`url`/`phone`/`numericRange`/`integer`/`isoDate`/`length`/`strongPassword`) ship + auto-register; `<FormItem>` gains `validator`/`validatorParams`/`validatorInvalidMessage`/`validatorInvalidSeverity` (legacy `pattern*` aliased + one-shot `deprecated-alias` warn); `App.registerValidator()` exposed via `AppContextDefs`. W5-2 done: `<FormValidator>` non-visual component (experimental, status="experimental") + `FormValidatorRegistryContext` provider in `FormWithContextVar`; cross-field validators run after preValidate, first-failure-wins; per-field/general errors merged via reused `BACKEND_VALIDATION_ARRIVED` action; `validator-throw` diagnostic on handler throws. W5-3 done: `extractServerValidationProblem()` maps RFC 7807 + Spring + Laravel + legacy `GenericBackendError.details.issues` into per-field/general results in the submit catch path; unknown field names emit `server-error-unmapped`; new `submitError(error, problem)` event on `<Form>`. W5-4 done: `decideSubmit()` guard + `submitPolicy` prop (`single-flight` default, `drop-while-running`, `queue` reserved); dropped submits emit `submit-while-busy` and fire `submitDropped(reason)`; per-submit `AbortController` created in `doSubmit`, exposed via new `Form.cancel()` API (cooperative — handlers must observe `$cancel`). W5-5 done (Step 5.1 CSRF/idempotency): `<Form csrfToken>` + `<Form idempotencyKey>` reactive props ship; built-in submit handler template injects them as headers via the new `$formHeaders` context var; `appGlobals.csrfHeaderName` (default `X-CSRF-Token`) + `appGlobals.idempotencyHeaderName` (default `Idempotency-Key`) + `appGlobals.requireFormCsrf` (default `false`) wired into `standalone.ts` docs; `csrf-token-missing` diagnostic fires on mutating submits when `requireFormCsrf` or `strictForms` is on (escalates to error under `strictForms`); pure helpers `buildSubmitHeaders()` + `shouldEmitCsrfMissing()` live in `components-core/forms/csrf.ts` with 17 unit tests in `tests/components-core/forms/csrf.test.ts`; per-submit `AbortController` now shared between outer `FormWithContextVar` and inner `Form` so `$formCancel.signal` + `$formCancel.aborted` + `$formCsrfToken` + `$formIdempotencyKey` + `$formHeaders` are available to custom `onSubmit` handlers; docs page updated with "CSRF and idempotency" section. **`appGlobals.strictForms` default flipped to `true`.** |
| UDC sandbox (#14) | ✅ | 2026-05 | **W5-9 → W8-1 fully shipped (2026-05).** `components-core/udc-sandbox/` module: `UdcContract` (parser-built on `CompoundComponentDef` from `<Prop>`/`<Event>`/`<Method>`/`<Slot>` declarations), `buildScopeGate()` proxy (out-of-contract reads → `udc-scope-leak`), capability enum (`fetch`/`websocket`/`eventsource`/`navigate`/`clipboard`/`randomBytes`/`log`/`mark`/`environment`) with `assertCapability()`/`narrowCapabilities()` wired through `evalIdentifier`/`evalMemberAccessCore` in `eval-tree-common.ts` (emits `udc-capability-missing`, throws `UdcCapabilityError` in strict); `validateUdcPropReferences()` (emits `udc-prop-undeclared`); trust modes (`trusted`/`untrusted`) honour `appGlobals.udcTrust` (`"open"`/`"review"`/`"strict"`) with `udc-untrusted-violation` per missing declaration/implicit capability; `loadManifest(udcName, contract, {sourceFile,searchDirs})` reads sibling `udc.manifest.json` / `<name>.udc.manifest.json` via runtime-guarded `fs` (browser-safe); `compareManifest()` emits `udc-manifest-mismatch` on shape drift; `collectUdcReport()`/`formatUdcAuditReport()` aggregate `kind:"udc"` trace entries for Inspector + CI. Host bindings: `CompoundComponent.tsx`, `rendering/Container.tsx`, `rendering/StateContainer.tsx` install scope gate + capability mapper into eval context. **`appGlobals.strictUdcSandbox` default flipped to `true` (W8-1)** — three host bindings now use `!== false`; set `false` for legacy warn-only behaviour. `ComponentRegistryEntry.udcContract` plumbed through `registerComponentRenderer` so cross-file analyzer rules can read declared contracts. Analyzer rule `udc-slot-undeclared` (warn → error in strict) catches consumer-side `slot="x"` not in the parent UDC's declared `slots`; legacy UDCs (no slot declarations) opt out. CLIs: `scripts/cli/udc-audit.ts` (GNU/JSON formats, `--fail-on-untrusted` for CI) and `scripts/cli/udc-declare.ts` (migration scaffolder — infers `<Prop>` blocks from `$props.x` references, optional `--write` in-place insert with `.bak` backup). Tests: `tests/components-core/udc-sandbox/contract.test.ts` + `runtime-integration.test.ts` (24 tests) + `tests/components-core/analyzer/rules/udc-slot-undeclared.test.ts` (6 tests). Docs: end-user page `/docs/managed-react/udc-sandbox` (NavLink + Page route in `website/src/Main.xmlui`); dev-guide chapter `35-udc-sandbox.md`; AI reference `.ai/xmlui/udc-sandbox.md`; `appGlobals` JSDoc in `standalone.ts` updated. |

---

## Open dimensions — plan cycle status

Rows are ordered by plan number. The Wave column is the *earliest* wave
that delivers work on this dimension (see `00-master-plan.md` Section 4).

| # | Dimension | Status | Wave | Last updated | Notes |
|---|---|---|---|---|---|
| 02 | Themevars namespace | 🔄 | W0 | 2026-05 | `themeNamespacePrefix` on `Extension`; canonical prefix table in `components-core/themevars/`; W1-5 done: `theming-missing-prefix` stub rule registered (analyzer rule body still a TODO), `theming-missing-prefix: "warn"` wired into all four create-app templates, AI docs + guide chapters 07/14 updated, end-user docs page shipped at `/docs/managed-react/themevars-namespace`. Remaining (deferred per master plan Appendix A item 1112+): lint-rule body (needs registry-side `componentName → expectedPrefix` mapping in `ComponentProvider`), migration of ≈16 extension packages' SCSS to canonical prefixes, default flip in next major. |
| 12 | Enforced versioning | 🔄 | W6 | 2026-05 | W6-4/5/6 done (Step 0 + Phases 1–4): `components-core/versioning/` module shipped with `VersioningDiagnostic` taxonomy (`deprecated-component`/`deprecated-prop`/`deprecated-event`/`deprecated-method`/`deprecated-value`/`removed-prop`/`renamed-prop`/`experimental-use`/`default-value-changed`/`internal-component-use`), `verifyVersioning()` walker (parse-time hook), `emitVersioningDiagnostics()` runtime echo with per-session dedup by `(componentName, propName, code)`, `applyRenames()` prop-rename helper, and `collectVersioningReport()`/`formatMigrationPlan()` aggregators backing the Inspector "Versioning" tab. `appGlobals.strictVersioning` (default `false`) escalates `removed-prop` + `internal-component-use` from `warn` to `error`; `deprecated-*` always stays at `warn`. `kind:"versioning"` registered on `XsLogEntry`. `ComponentPropertyMetadata`/`ComponentEventMetadata`/`ComponentApiMetadata` extended with `deprecatedSince`/`removedIn`/`replacement`; `ComponentPropertyMetadata` adds `valueAliases` + `defaultValueChangedIn`; `ComponentMetadata` adds `renamedProps`. Build-time tooling under `scripts/api-diff/`: `extract.ts` produces deterministic `ApiSurface` JSON, `diff.ts` classifies deltas (`component/prop/event/method-added/removed`, `prop-required-added/relaxed`, `prop-type-changed`, `prop-default-changed`, `prop-enum-removed`, `component-status-changed`) to `patch`/`minor`/`major` honouring `removedIn` coverage, and `suggest-changeset.ts` checks staged changesets against the required bump with `--allow-patch` override (preserves repo's `patch`-default convention). 35 unit tests under `tests/components-core/versioning/` + `tests/scripts/api-diff/`. Remaining: LSP wiring for the verifier, parse-time hook in renderChild, doc badges in MetadataProcessor, `valueAliases`/`preserveLegacyDefaults` rewrite in `valueExtractor`, release-guard CI workflow, docs chapter (35-versioning.md), strict default flip in W8. |
| 15 | Audit-grade observability | 🔄 | W0 | 2026-05 | W0-3 done: `AuditPolicy`, redactor/sampler/correlation/sink stubs in `components-core/audit/`; W2-1 done: W3C Trace Context (`correlation.ts` with span stack, `BOOT_TRACE_ID`, `injectTraceparent`); RestApiProxy injects `traceparent` for same-origin requests; XsLogEntry stamped with `spanId`/`parentSpanId`; W2-2 done: dot-path PII redactor (wildcards, drop>hash>mask precedence, FNV-1a hash) + content heuristics (JWT/email/CC/SSN/API-key/phone/IPv4) + `audit` field on `ComponentPropertyMetadata`; W2-3 done: head + tail sampling with LRU memoization, `pushXsLog` overflow modes (`drop-oldest`/`drop-newest`/`block`); W2-4 done: OTLP/JSON sink (1s batch, exp-backoff, beacon on unload) + console group sink; W3-5 done: `PropertyDef.audit` extended with per-key `fieldPolicies` (`{ classification, defaultRedaction }`); component metadata sweep applied (`TextBoxMd.initialValue` sensitive→hash, `PasswordMd.initialValue` secret→mask, `APICallMd.headers` secret→mask with per-header `Authorization`/`Cookie` overrides, `FormMd.data` sensitive→hash); `buildBaselineRules(propsMetadata)` emits per-`fieldPolicies` selectors plus a `${propName}.*` wildcard fallback. |
| 16 | Concurrent-state determinism | 🔄 | W4 | 2026-07 | Determinism completion pass landed: guide chapter, renderer render-phase markers, promise-based FIFO/concurrent scheduler with per-trace queues, handler hot-path scheduling, reorder/convergence diagnostics, `<App scheduler>` + `maxQueuedPerTrace`, replay trace comparator, stable spacing serialization helper, and ordered-key iteration used by `Items`. W7-3 shipped `determinism-floating-point-token` and `determinism-iteration-order-symbol` analyzer rules; W7-4 shipped `xmlui replay <expected.json> [actual.json]` CLI subcommand. W8-1 done: `appGlobals.strictDeterminism` default flipped from `false` to `true` (3 read sites in `AppContent.tsx` now use `!== false`); default scheduler is now `"fifo"` and scheduler `strict` mode is on by default. Remaining: Inspector "Replay this trace" UI (Step 4.2, devtools package). |

---

## How to update this file

When a plan phase ships:

1. Change ⬜ to 🔄 when work starts; change 🔄 to ✅ when all phases for
   the dimension are complete.
2. Fill in the *Last updated* column with `YYYY-MM`.
3. Add a one-line note (what shipped, what remains if partial).
4. If this is a Wave 8 strict-default flip, copy the row into the "Closed"
   table above and remove it from the open table.
