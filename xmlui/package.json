{
  "name": "xmlui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:standalone": "vite build --config vite.standalone.config.ts",
    "prepare:standalone-samples": "npm run build:standalone && node scripts/prepare-standalone-samples.mjs",
    "serve:standalone-samples": "npm run prepare:standalone-samples && node scripts/standalone-static-server.mjs",
    "test": "vitest run tests/compiler",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^6.0.0",
    "vite": "^8.0.0",
    "typescript": "^5.7.3",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "@playwright/test": "1.57.0",
    "@types/node": "^22.18.13",
    "@types/react": "18.2.23",
    "@types/react-dom": "18.2.8",
    "vitest": "^4.1.0"
  }
}
